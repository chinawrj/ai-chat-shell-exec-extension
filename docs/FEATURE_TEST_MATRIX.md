# Feature Test Matrix

This table is the required map between product features and automated test cases. When a feature or test case is added, removed, or renamed, update this file in the same change.

`./scripts/test_all.sh` is the single command for the full automated suite. It runs static checks, shell syntax checks, whitespace checks, every `tests/*.test.js` file, and the Chrome extension e2e test.

| Feature or invariant | Test cases | Coverage notes |
| --- | --- | --- |
| Root and `extension/` manifests stay behaviorally aligned for either unpacked load path, and user-visible content/background/release versions stay unified. | `tests/manifest_consistency.test.js` | Compares manifest version, key, permissions, background worker, popup, content script wiring, content script version, background version message support, shell-health startup checks, `tmux-ensure`/`tmux-reset-forai` wiring, state-health wiring, changelog entry, and release-note file. |
| Default enabled hosts and automatic chain limit stay aligned across background, content, and popup code. | `tests/default_enabled_hosts.test.js` | Enforces `chatgpt.com`, `m365.cloud.microsoft`, and `maxChainCalls = 100` in all duplicated defaults. |
| Background settings migration preserves existing user choices while migrating legacy defaults. | `tests/background_settings_migration.test.js` | Covers legacy enabled-host and chain-limit migration behavior. |
| Background file-helper message handling forwards file writes over WebSocket only after server protocol health passes, and records ledger state without duplicate blocking. | `tests/background_write_file_message.test.js` | Covers write-file payload shape, success response handling, repeated same-key forwarding, audit ledger updates, and protocol health gating before WebSocket forwarding. |
| Background board-helper message handling forwards board commands over WebSocket only after server protocol health passes, and records ledger state without duplicate blocking. | `tests/background_write_file_message.test.js` | Covers `run-board` payload shape, success response handling, target recording, repeated same-key forwarding, and protocol mismatch blocking without sending WebSocket payloads. |
| Background agent-hub message handling forwards only allowlisted `agent-*` messages over WebSocket after server protocol health passes. | `tests/background_agent_message.test.js` | Covers `agent-register`, `agent-register-tmux-ai`, `agent-send`, `agent-poll`, `agent-ack`, and `agent-reply` forwarding through the existing health gate, plus rejection of unsupported agent message types before WebSocket forwarding. |
| Local agent hub keeps an in-memory roster and mailbox for multi-tab master/slave experiments. | `tests/agent_hub.test.js`, `tests/content_agent_delivery.test.js`, `tests/chrome_extension_e2e.test.js` | Covers agent registration, listing, pending counts, send, poll, ack, poll-based heartbeat, stale roster pruning, content auto re-registration after roster loss, visible floating-panel pending delivery while the AI page composer/send control is not ready, explicit cached-message/next-action text for pending delivery, reinsert after a page redraw removes the pending prompt, ack-only retry after the page was already sent with no-resend explanation, local pending cleanup after the page agent profile changes, duplicate insertion protection while a message waits for the send button, invalid agent IDs, invalid roles, unregistered sender rejection, missing recipients, local test page bridge coverage for register/send/poll/ack, floating-panel role registration, slave inbox delivery into the local composer, and two-tab slave-to-master reply delivery. |
| Tmux AI agents can register an explicit tmux pane as a master/slave runtime, receive task prompts, and reply through the CLI instead of tmux output parsing. | `tests/tmux_agent_integration.test.js`, `tests/agent_reply_cli.test.js`, `tests/background_agent_message.test.js`, `tests/content_agent_delivery.test.js`, `tests/chrome_extension_e2e.test.js`, opt-in `tests/real_claude_tmux_slave_e2e.test.js` | Covers `surface: tmux-ai`, `replyMode: cli`, master floating-panel registration of tmux-ai slaves, self-explanatory registration errors for missing master/slave id/tmux target, agent setup Check diagnostics for missing and ready tmux-ai configurations, tmux target validation, prompt delivery into a registered pane, per-task short reply script generation, delivery metadata that exposes reply body file, reply script file, short reply command, full CLI command for diagnostics, and next step, reply body-file validation, CLI WebSocket payload forwarding, CLI failure hints and next actions, `agent-reply` insertion into the master mailbox, pending web-page delivery until the recipient page is ready, preservation of unreplied tmux-ai tasks during mailbox pruning for long-running work, duplicate reply rejection, wrong-recipient/task-id reply rejection, browser e2e coverage for web master panel -> tmux-ai slave -> CLI reply -> web master delivery, and opt-in proof with a real Claude Code tmux pane. |
| Project-level Claude Code skill guides tmux-hosted Claude slaves to complete tasks and report back through the short reply script. | `tests/claude_skill.test.js` | Covers `.claude/skills/tmux-ai-slave-reply/SKILL.md` frontmatter, auto-trigger description, reply-file/short-reply-command workflow, preference for the short script over reconstructing long CLI flags, `ok: true` completion gate, and duplicate/error handling guidance. |
| Extension release version, server protocol version, helper protocol version, and visual protocol metadata stay separate; core diagnostics remain concise when vision is unused, while server health still exposes visual metadata for vision-capable flows. | `tests/protocol_metadata.test.js`, `tests/background_write_file_message.test.js`, `tests/background_vision_message.test.js`, `tests/popup_config.test.js`, `tests/manifest_consistency.test.js` | Covers `serverProtocolVersion = 4`, `helperProtocolVersion = 2`, `visualProtocolVersion = 1`, release metadata in `/health`, background rejection of old, missing-helper, or helper-mismatched servers before shell/file/board/vision WebSocket forwarding, concise popup health text when vision is unavailable, and source guards for floating-panel protocol status text. |
| Plain text shell helper parsing accepts `ai-helper-shell-start`, command body, and `ai-helper-shell-end`; every line between start and end is command text, so heredocs and `cat <<EOF` file creation are preserved. AI-facing helper examples use four-backtick fences while rendered marker-only blocks remain supported, and closed four-backtick fenced shell helpers can recover from a missing `ai-helper-shell-end`. | `tests/content_plain_text_shell_call.test.js` | Covers parsing, extracted helper blocks inside surrounding text and four-backtick markdown examples, no-target shell helpers, legacy target-looking lines treated as command text, heredoc command preservation, validation, command body preservation, fenced missing-end recovery, matching payload hashes with explicit end markers, and non-recovery when either fence boundary is missing. |
| Plain text file helper parsing accepts `ai-helper-file-start`, filename, exact content, and `ai-helper-file-end`; closed four-backtick fenced file helpers can recover from a missing `ai-helper-file-end`. | `tests/content_plain_text_shell_call.test.js` | Covers JSON-like file content as plain text, trailing blank lines, file validation, file helper result formatting, and fenced missing-end recovery with trailing blank content preservation. |
| Plain text board helper parsing accepts `ai-helper-board-start`, exactly one board command line, and `ai-helper-board-end`; closed four-backtick fenced board helpers can recover from a missing `ai-helper-board-end`. | `tests/content_plain_text_shell_call.test.js` | Covers board parsing, identity suffixes, semantic key differences, fenced missing-end recovery, empty command rejection, multiline rejection, and copied-helper-marker rejection. |
| Plain text agent-message helper parsing accepts `ai-helper-agent-message-start`, `to` and optional `task-id` headers, a blank-line-separated body, and `ai-helper-agent-message-end`; closed four-backtick fenced agent-message helpers can recover from a missing end marker. | `tests/content_plain_text_shell_call.test.js` | Covers agent-message parsing, identity suffixes, semantic key differences, safe recipient validation, task-id validation, empty body rejection, fenced missing-end recovery, helper detection hints, slave reply prompt formatting, default master agent ID suggestions, and roster summary formatting. |
| AI master pages can autonomously discover available slave agents and inspect delegated task state through read-only `ai-helper-agent-roster-*` and `ai-helper-agent-task-status-*` helpers. | `tests/content_plain_text_shell_call.test.js`, `tests/content_agent_delivery.test.js`, `tests/content_last_shell_call_candidate.test.js`, `tests/agent_hub.test.js`, `tests/background_agent_message.test.js` | Covers roster/status helper parsing, filters, validation, candidate detection hints, rejection of malformed query helpers, repeatable query dedup behavior so repeated roster/status checks are not suppressed by semantic dedup, background forwarding of `agent-list` and `agent-task-status`, server `agent-list` capability metadata including `surface`, `replyMode`, `pendingCount`, `canReceiveTask`, `capabilities`, and `lastSeenAgeMs`, content dispatch from helper to hub query, formatted `shell-output` for roster and task status, and AI-oriented recovery hints for missing recipients. |
| JSON helper requests and old `ai-helper-start-shell` aliases remain unsupported. | `tests/content_plain_text_shell_call.test.js` | Covers language-hint rejection and shell-call validation rejection for old markers. |
| Optional helper identity suffixes distinguish otherwise-identical shell and file helper payloads. | `tests/content_plain_text_shell_call.test.js`, `tests/chrome_extension_e2e.test.js` | Covers suffixed shell and file parsing, semantic key differences, malformed suffix rejection, unsuffixed payload hashes, and a real suffixed shell e2e request. |
| Shell-output formatting is compact but still preserves command identity for the minimal frontend loop guard and user diagnosis. | `tests/content_shell_output_format.test.js` | Covers short commands, long commands, multiline commands, `cmdHash`, and same-command detection. |
| Backend command ledgers are non-blocking audit records: repeated completed/running call keys still run. | `tests/background_write_file_message.test.js`, `tests/background_vision_message.test.js`, `tests/server_dedup_force.test.js`, `tests/tmux_visual_run_integration.test.js` | Covers browser and server claim behavior for repeated same-key requests, including completed and running ledger records, with no duplicate skip response. |
| Force call (`callMeta.force=true` or top-level `force=true`) records `forced: true` for audit and remains accepted across browser + server handlers. | `tests/background_write_file_message.test.js`, `tests/server_dedup_force.test.js` | Covers force metadata recording, payload forwarding, top-level force consistency for browser handlers, and ledger `forced` fields without relying on backend dedup bypass. |
| Manual `Force run` execution preserves pending forced retry behavior, user-facing force button metadata, and selects the latest assistant helper block even when that message quotes prior `shell-output`. | `tests/content_force_run.test.js`, `tests/content_last_shell_call_candidate.test.js` | Source-level guard for force-run function names plus runtime candidate-selection regression coverage for mixed quote+helper messages, pure quoted output, active-call pending force retention, and force scan dispatching the newest helper cmd. |
| Floating panel "detected helper" debug view always reflects the most recent helper block, including while the chat thread is still streaming or before the quiet-period gate fires. | `tests/content_last_shell_call_candidate.test.js` | Covers the post-scan debug update plus a streaming-phase regression that asserts the panel shows the newest helper cmd and not an earlier one when scan early-returns due to thread text changes. |
| Popup settings UI can load, save, normalize enabled sites, list tmux targets, display default `ForAI` host/board/cwd state, and export/import portable config without ledgers. | `tests/popup_config.test.js` | Covers tmux target display, default workspace summary, site normalization, config export/import, clamping, and exclusion of command ledgers. |
| Tmux target parsing and resolution accept pane id, `session:window.pane`, and unique window names while rejecting ambiguous names. | `tests/tmux_helpers.test.js` | Covers pane parsing, target resolution, tmux socket argument building, and tmux environment socket extraction. |
| Board helper target selection, prompt extraction, normalized pipe-log capture, and board helper examples stay stable. | `tests/tmux_helpers.test.js` | Covers default `board` window resolution, configured target override, missing/ambiguous board errors, ANSI/CR normalization, prompt tail matching, output truncation, and single-line board validation. |
| Board helper tmux integration probes the prompt before sending commands and refuses promptless panes. | `tests/tmux_board_integration.test.js` | Uses a private tmux socket with fake `board` panes to cover successful prompt-based capture and probe failure without sending the formal command. |
| Page startup and tmux target listing ensure a default `ForAI` tmux session with `host` and `board` windows; new default windows use the configured cwd; reset recreates only the default `ForAI` session; shell helpers always run in `ForAI:host`, and board helpers default to `ForAI:board`. | `tests/tmux_forai_setup_integration.test.js`, `tests/tmux_helpers.test.js`, `tests/content_plain_text_shell_call.test.js`, `tests/popup_config.test.js`, `tests/manifest_consistency.test.js`, `tests/feature_test_matrix.test.js` | Uses a private tmux socket to cover automatic session/window creation, realpath cwd policy, idempotent setup, reset with and without an existing session, default shell execution, heredoc file creation followed by later commands in one run, ignoring legacy shell target fields, default target resolution, parser support for targetless shell helpers, popup default summary, content/background reset wiring, exact tmux session targeting so `ForAI` does not match `ForAI-*`, and docs/agent-guide protection against reintroducing the obsolete shell target line. |
| Agent pages can route shell helpers to an isolated per-agent tmux workspace while non-agent pages continue to use `ForAI:host`. | `tests/tmux_forai_setup_integration.test.js`, `tests/chrome_extension_e2e.test.js` | Covers `agentId: slave-a` mapping to `ForAI-slave-a:host`, exact tmux target handling with both default and per-agent sessions present, and local test page e2e proof that an agent-profiled tab runs shell helpers in the per-agent tmux session before unregistering and returning to the default non-agent path. |
| Runtime state directory health is required before shell server startup and command execution; project `.state` is the default runtime state directory, safe conflicts are auto-repaired with `.broken-*` backups, `AI_CHAT_SHELL_STATE_DIR` remains an explicit override, and macOS LaunchAgent auto-start stays removed. | `tests/state_dir.test.js`, `tests/manifest_consistency.test.js`, `scripts/doctor.sh`, `scripts/test_all.sh` | Covers default state path selection, missing state auto-creation, state path as a file auto-repair, required subpath conflict auto-repair, corrupted ledger backup and recovery, state-log path repair, installer compatibility cleanup plus foreground launch, unwritable state where supported, health fields, content/background health wiring, and shell syntax checks for startup/cleanup scripts and state-log handling. |
| Server command claims are not left stuck as `running` when post-claim execution throws. | `tests/server_dedup_force.test.js` | Covers post-claim failure cleanup to `failed` with bounded error metadata, preserving future retry behavior. |
| Doctor diagnostics check Node.js, tmux, default `ForAI` host/board visibility, manifest ID, server origin policy, server protocol version, helper protocol version, state directory health, and default cwd validity without mutating tmux state. | `scripts/doctor.sh`, `scripts/test_all.sh` | Shell syntax checks cover the script in the full suite; manual doctor runs exercise live tmux/server state and respect `AI_CHAT_SHELL_STATE_DIR`, `AI_CHAT_SHELL_TMUX_SOCKET`, `AI_CHAT_SHELL_TMUX_SESSION`, `AI_CHAT_SHELL_HOST_WINDOW`, `AI_CHAT_SHELL_BOARD_WINDOW`, and `AI_CHAT_SHELL_FORAI_CWD`; offline guidance points to `./scripts/start_shell_server.sh`. |
| Internal direct tmux visual-run test mode clears pane history, executes one command through `/bin/sh -c`, marks completion via tmux window name, and dumps the tmux buffer only when `AI_CHAT_SHELL_ENABLE_DIRECT_VISUAL_TMUX=1` is set. | `tests/tmux_visual_run_integration.test.js` | Uses a private tmux socket to cover default production rejection, explicit test-mode enablement, `&&`/`||` compatibility, nonzero exit propagation, stale history removal, done-window marker detection, capture-pane output, repeated same-key execution without backend duplicate blocking, and single-line validation. |
| Default tmux target failures produce a helper-readable failure without requiring AI helper blocks to name a target. | `tests/tmux_helpers.test.js` | Covers default target failure response shape and example helper block. |
| File helpers write only a single file name under the Downloads directory. | `tests/tmux_helpers.test.js` | Covers safe path resolution, traversal rejection, byte counts, and file content writes. |
| Tmux run output extraction finds start/done markers, exit codes, timeout partial output, and truncation. | `tests/tmux_helpers.test.js` | Covers complete, failing, partial, missing-marker, and truncated command output cases. |
| macOS UI-window vision control exposes structured health, local Terminal/Ghostty visual tmux candidate discovery, and high-level visual run requests by default; generic window listing/capture/type/key, message-level OCR, and Terminal self-test primitives require `AI_CHAT_SHELL_ENABLE_LOW_LEVEL_VISION=1` for local manual tests. | `tests/vision_messages.test.js`, `tests/background_vision_message.test.js` | Uses a fake macOS vision helper to cover low-level default rejection, low-level explicit test-mode validation, helper missing, non-macOS, permission denied, invalid window, local visual candidate filtering for Terminal.app/Ghostty, unsupported app rejection, app mismatch rejection, long input, unsafe control characters, OCR response shape, allowed special keys, background low-level blocking, background stale-server gating, and repeated same-key forwarding for command-capable vision messages. |
| Vision-only tmux command output can be reconstructed from OCR pages in supported local macOS tmux surfaces, currently Terminal.app and Ghostty windows, rather than direct tmux buffer access. | `tests/vision_messages.test.js` | Covers a successful fake-helper `vision-tmux-ocr-run-line` through known-exit done-marker wait, copy-mode pagination, OCR page stitching, nonzero exit propagation, start-marker detection, temporary high tmux history limit reporting, conservative pagination-end detection, OCR page failure reporting, repeated-page lossy/truncation reporting, lossy cleanup/truncation reporting, missing-start truncation, and max-page truncation; also covers bbox-based OCR row reconstruction, OCR-safe tmux status marker matching, unknown marker-status handling, PageDown key validation, cleanup of tmux control/status lines, supported-app filtering, unsupported-app rejection, and command wrapping that preserves marker execution when the command contains `#`; `scripts/run_terminal_vision_self_test.sh` is a manual macOS GUI token-loop check for screenshot/OCR/input. |
| WebSocket text frame parsing handles fragmented, coalesced, extended-length, masked client, and server frames. | `tests/server_websocket_frames.test.js` | Covers low-level frame decoder and encoder behavior used by the local server. |
| Chrome extension e2e flow works with a real unpacked extension, Chromium-family browser, default no-target shell helper, file helper, and optional screenshots. | `tests/chrome_extension_e2e.test.js` | Launches or reuses the local shell server and HTTPS test page, inserts a no-target shell helper block, verifies returned `shell-output` includes the default host target name, verifies file helper output, and writes screenshots when requested. |
| Release packaging creates matching source and extension archives for the selected manifest version; clean releases package `HEAD`, while `ALLOW_DIRTY=1` local packages include the current worktree plus untracked non-ignored files. | `scripts/package_release.sh` | Guards against stale source archives during local release validation and keeps generated zip checksums under `dist/release/v*/SHA256SUMS.txt`. |
| Feature/test coverage documentation stays in sync with all committed automated test files. | `tests/feature_test_matrix.test.js` | Fails if any `tests/*.test.js` file is missing from this matrix or if this matrix is not referenced from project docs. |
| Full automated checks and tests can be run with one command on macOS and Ubuntu. | `scripts/test_all.sh`, `tests/feature_test_matrix.test.js` | The runner performs JavaScript syntax checks, per-script shell syntax checks, `git diff --check`, every Node test, and the Chrome extension e2e test. |
