# Feature Test Matrix

This table is the required map between product features and automated test cases. When a feature or test case is added, removed, or renamed, update this file in the same change.

`./scripts/test_all.sh` is the single command for the full automated suite. It runs static checks, shell syntax checks, whitespace checks, every `tests/*.test.js` file, and the Chrome extension e2e test.

| Feature or invariant | Test cases | Coverage notes |
| --- | --- | --- |
| A helper proven live at generation time remains eligible across one exact provisional-to-opaque-id route assignment even when the scanner is still awaiting its outer pending-state/settings read and the host performs only `pushState` with no later DOM mutation. The assigned route schedules its own settled rescan. The proof is candidate- and authored-turn-bound: changed user text, an unproved replacement turn, or a permanent-conversation A-to-B route must not inherit it. M365 requires the original DOM roots; ChatGPT may rebind only one unique stable-ID replacement turn. A same-semantic observer redraw or stable-ID body replacement transfers a completed/active processed claim only after exact route/turn validation, so releasing shell or Skill backend locks cannot lose the result or dispatch the replacement root twice. A tentative claim still awaiting settings, approval, profile, or local state before backend dispatch is instead released for one settled retry and must not be restored as processed. | `tests/content_last_shell_call_candidate.test.js`, `tests/content_skill_load_dispatch.test.js`, `tests/chrome_extension_e2e.test.js` | Focused shell, Skill, and Draw.io cases carry unchanged M365 `/chat` turns and reject changed user text; additional shell/Skill races cover observer-invalidated render generations, duplicate stable-ID rejection, one backend-in-flight ChatGPT route plus full turn replacement, completed-then-rerendered exact-one behavior, pre-backend active/preparing claim release, and the processed-claim transfer after same-lifecycle body redraw. Independent real-Chrome M365 shell and installed-Skill pages gate the outer settings read, retain the exact completed user/assistant turn, change only the URL, then assert one backend request, one AI reply, no duplicate execution, no composer leak, and no remaining helper/reconciliation lock. A same-text M365 replacement-root page asserts zero runtime requests, marker executions, replies, pending entries, and leaked locks. |
| Composer cancellation records and immediately persists a monotonic pending-delivery boundary when composer ownership is first lost, including trusted Enter/LineBreak emptying and the submitted-unconfirmed interval before deletion can be distinguished from submission. It performs a synchronous final origin/attempt preflight after its asynchronous guard, then removes only identity-matching entries at or before that boundary. Thus delayed cancellation recognition, reload/route recovery, same-URL replacement, and asynchronous proof waits cannot authorize stale Skill side effects or erase a genuinely newer helper. | `tests/content_route_delivery.test.js`, `tests/content_skill_load_dispatch.test.js`, `tests/chrome_extension_e2e.test.js` | Covers deterministic old-cancellation/new-helper interleaving, the real trusted-empty caller followed by a newer helper before cancellation recognition, durable boundary restoration across reload, a real nonempty trusted replacement held at the proof await whose snapshot relies only on automatic boundary persistence, multiple older entries, same-call-id replacement, the post-guard microtask replacement window, exact stale-Skill cleanup, preservation of later/current entries, and real-Chrome delivery of the newly rendered helper after user cancellation. |
| Root and `extension/` manifests stay behaviorally aligned for either unpacked load path, Chrome 116 is the minimum supported browser, and user-visible content/background/release versions stay unified. | `tests/manifest_consistency.test.js` | Compares manifest version, minimum Chrome version, key, permissions, background worker, popup, content script wiring and order, sandbox CSP/pages, web-accessible packaged viewer resources, content script version, background version message support, shell-health startup checks, `tmux-ensure`/`tmux-reset-forai` wiring, state-health wiring, changelog entry, and release-note file. |
| Default enabled hosts, automatic chain limit, and 3-minute shell output-idle timeout stay aligned across extension and server defaults. | `tests/default_enabled_hosts.test.js` | Enforces `chatgpt.com`, `m365.cloud.microsoft`, `maxChainCalls = 100`, and `defaultTimeoutMs = 180000`. |
| `AI_CHAT_SHELL_ENV_FILE` points to one freshly read, real, non-symlinked, bounded UTF-8 declaration file whose values are overlaid on ai-helper shell subprocesses, trusted Skill install/uninstall subprocesses, and exact-SHA Skill load expansion. Parsing is data-only and fail-closed; values do not enter logs, ledgers, unrelated helpers, or the server process environment. Every env-file name is automatically load-eligible without a duplicate allowlist entry and overrides a same-named process value; only placeholders referenced by an installed Skill can enter its bounded AI load reply. Extra process-environment names still require `AI_HELPER_SKILL_ENV_ALLOWLIST`, Claude placeholders remain literal, server-owned root placeholders cannot be spoofed, and expansion stays one-pass. Shell injection uses the existing private transient tmux runner lifecycle. Shell duplicate identity includes the file-content SHA so changing the file makes an otherwise identical command executable again, while environment-only changes never affect catalog SHA/version. An optional adjacent `uninstall.sh` has the same trusted-click, exact-SHA, immutable-snapshot, serialized output-idle lifecycle as `install.sh`; exit 0 atomically clears the exact installed record before catalog refresh. Failure does not proactively clear an unchanged identity, while normal receipt reconciliation still resets any identity changed by the failed script or another writer. | `tests/execution_env.test.js`, `tests/tmux_shell_lifecycle_integration.test.js`, `tests/server_skill_install.test.js`, `tests/server_skill_catalog.test.js`, `tests/server_skill_protocol.test.js`, `tests/background_skill_sync.test.js`, `tests/content_skill_helper.test.js`, `tests/content_compact_panel.test.js`, `tests/skill_install_result_page.test.js`, `tests/chrome_extension_e2e.test.js` | Covers absent configuration, comments/export/empty/quoted/Unicode/prototype-named values, safe shell quoting, duplicate/invalid names, malformed quotes/escapes, invalid UTF-8, nulls, symlinks, size/entry/value limits, fresh rereads, name/value-free errors, real tmux export, unchanged-environment dedup, changed-environment execution, malformed pre-execution rejection, minimal lifecycle base environment plus explicit overlay, env-file-only Skill expansion without duplicate allowlisting, same-name override, one-pass replacement, hot reload without catalog/version churn, Claude/runtime placeholder precedence, non-allowlisted process-variable preservation, malformed/missing-file load rejection without value/path disclosure, real `/bin/sh` install/uninstall snapshots, missing/stale/failing uninstallers, successful and failed uninstall concurrent with SKILL.md/install.sh changes, installed-state/version transitions, protocol path redaction, trusted/cancelled/double-click UI, failure-token isolation, heartbeat/no-watchdog behavior, and real unpacked-Chrome shell, Skill lifecycle, fresh load, hot-reload, and malformed-load checks. |
| Background settings migration preserves existing user choices while migrating legacy defaults. | `tests/background_settings_migration.test.js` | Covers legacy enabled-host and chain-limit migration, upgrades the old 30-second default timeout to 3 minutes, and preserves custom timeout choices. |
| Background WebSocket forwarding passes file, shell, board, status, and shell-run-control messages only after server protocol health succeeds; long shell and board channels use 20-second activity heartbeats and no browser runtime watchdog, shell progress frames are forwarded to the originating tab without closing the final-response channel, and read-only status recovery never creates a new execution claim. A bounded internal UI-delay response supplies the original send actuator with visibility-independent short waits. | `tests/background_write_file_message.test.js`, `tests/tmux_forai_setup_integration.test.js`, `tests/chrome_extension_e2e.test.js` | Covers write-file and command payload shapes, success handling, repeated same-key forwarding, non-blocking audit ledger updates, protocol health gating, heartbeat send/cleanup behavior, shell/board watchdog disabling, output-idle progress plus continue/terminate control, status-result audit updates without command replay, and a nonsynchronous bounded background delay response. |
| Background board-helper message handling forwards board commands over WebSocket only after server protocol health passes, and records ledger state without duplicate blocking. | `tests/background_write_file_message.test.js` | Covers `run-board` payload shape, named board forwarding, success response handling, target recording, repeated same-key forwarding, and protocol mismatch blocking without sending WebSocket payloads. |
| Background agent-hub message handling forwards only allowlisted `agent-*` messages over WebSocket after server protocol health passes. Active web-agent profiles are stored in extension-owned per-tab session state so refresh survives page-owned storage loss without leaking the role into a new tab. | `tests/background_agent_message.test.js`, `tests/content_agent_delivery.test.js`, `tests/chrome_extension_e2e.test.js` | Covers `agent-register`, `agent-register-tmux-ai`, `agent-send`, `agent-poll`, `agent-ack`, and `agent-reply` forwarding through the existing health gate, unsupported message rejection, per-tab profile validation/read/write, content hydration, and a real browser refresh after clearing page `sessionStorage`. |
| Local agent hub keeps an in-memory roster and mailbox for multi-tab master/slave experiments. Agent delivery and helper scanning share exact composer ownership without treating pending UI work as command execution. Each agent prompt has a one-write limit; user removal/replacement cancels composer delivery, while any navigation after the first agent-prompt composer write cancels send ownership because inbound prompts have no authored-turn proof. The agent poll still observes route-only URL changes before loading route-scoped state. Profile changes still cancel the old lifecycle. | `tests/agent_hub.test.js`, `tests/content_agent_delivery.test.js`, `tests/content_last_shell_call_candidate.test.js`, `tests/content_route_delivery.test.js`, `tests/chrome_extension_e2e.test.js` | Covers agent registration, listing, pending counts, send, poll, ack, heartbeat and stale pruning, tab-local activation, content re-registration, pending delivery UI, outer composer ownership validation, one v0.8.9 actuator invocation per page lifecycle, manual-submission observation without actuator restart, atomic draft preservation, reload-safe sent-but-unacked state, deletion without reinsertion or another send attempt, cancellation-ack retry without composer mutation, exact-text SPA route-only transfer without DOM mutation or rewrite, profile cancellation, helper deferral, exact-text ownership, and response-loss idempotency: an identical `messageId` plus semantic payload returns the original web/tmux-ai envelope without redelivery, while changed payload conflicts. Also covers invalid IDs/roles/recipients, local bridge flows, panel setup, master delegation, slave composer delivery, and browser proof that quoted reply templates do not auto-send placeholder results. |
| Tmux AI agents can register an explicit tmux pane as a master/slave runtime, receive task prompts, and reply through the CLI instead of tmux output parsing. | `tests/tmux_agent_integration.test.js`, `tests/agent_reply_cli.test.js`, `tests/background_agent_message.test.js`, `tests/content_agent_delivery.test.js`, `tests/chrome_extension_e2e.test.js`, opt-in `tests/real_claude_tmux_slave_e2e.test.js` | Covers `surface: tmux-ai`, `replyMode: cli`, master floating-panel registration of tmux-ai slaves through the real Refresh dropdown path and roster state, self-explanatory registration errors for missing master/slave id/tmux target, agent setup Check diagnostics for missing slaves, browser-only ready states, ready tmux-ai configurations, stale tmux-ai panes, and re-registration hints for stale pane ids, async roster validation that marks missing tmux-ai panes as `stale` and `canReceiveTask=false`, send-time stale marking for unavailable tmux targets, tmux target validation, prompt delivery into a registered pane, per-task short reply script generation, delivery metadata that exposes reply body file, reply script file, short reply command, full CLI command for diagnostics, and next step, reply body-file validation, CLI WebSocket payload forwarding, CLI failure hints and next actions, `agent-reply` insertion into the master mailbox, pending web-page delivery until the recipient page is ready, preservation of unreplied tmux-ai tasks during mailbox pruning for long-running work, duplicate reply rejection, wrong-recipient/task-id reply rejection, browser e2e coverage for web master panel -> tmux-ai slave -> CLI reply -> web master delivery, and opt-in proof with a real Claude Code tmux pane. |
| Project-level Claude Code skill guides tmux-hosted Claude slaves to complete tasks and report back through the short reply script. | `tests/claude_skill.test.js` | Covers `.claude/skills/tmux-ai-slave-reply/SKILL.md` frontmatter, auto-trigger description, reply-file/short-reply-command workflow, preference for the short script over reconstructing long CLI flags, `ok: true` completion gate, and duplicate/error handling guidance. |
| The bundled `skill-creator` creates or updates focused Claude-compatible Skills in the AI Chat Shell Exec local catalog without assuming a fixed runtime directory. It creates a deterministic non-interactive `install.sh`, requires Skill-relative paths to use the guaranteed Skill-directory cwd rather than snapshot `$0`, explains that new or changed Skills remain unavailable until the user installs them from `View Skills`, and never tries to install through an AI helper. At exact-SHA load, the shell server injects authoritative resolved roots JSON and the configuration source through non-spoofable runtime placeholders. | `tests/skill_creator_skill.test.js`, `tests/server_skill_catalog.test.js`, `tests/package_release_version.test.js` | Loads the real bundled Skill through `SkillCatalogService` using both `AI_HELPER_SKILL_PATHS` and `AI_HELPER_SKILL_PATH`, verifies plural/multi-root/empty/default and plural-empty precedence, proves runtime root/source substitution uses the server resolution even when environment values try to spoof it, checks the shipped `$PWD`-based safe installer and immutable-snapshot guidance, rejects `$0`-based path advice, unresolved placeholders, and a fixed default directory in the loaded body, and confirms the Skill, installer, and focused tests ship in the source release archive. |
| Local Claude-compatible `SKILL.md` catalogs are discovered only through the shell server's bounded high-level file service, never through shell/tmux execution. Raw file SHA-256 values produce the aggregate catalog SHA without a separate effective SHA; schema-validated version state advances when that aggregate changes. Brief cached status snapshots coalesce health/poll traffic, while list/load/rescan and final ACK checks are fresh. Catalog parsing rejects malformed frontmatter, duplicate names, unsafe links, oversized/non-UTF-8 files, total-size excess, serialization excess, and stale load requests without exposing absolute paths publicly. Root count, visited entries, traversal issues, and returned diagnostics are bounded. Skill bodies are loaded only by exact id plus full SHA, then perform one-pass environment substitution from default/explicit process allowlists plus every freshly parsed env-file name under an incremental output budget without executing text. The server sizes the exact final fenced composer reply and fails closed before returning any expanded body that would exceed 500,000 characters. | `tests/server_skill_catalog.test.js`, `tests/server_skill_protocol.test.js`, `tests/chrome_extension_e2e.test.js` | Covers default and configured roots, recursive discovery, deterministic length-prefixed aggregate hashing with duplicate hashes retained, move/rename stability, persisted monotonic versions plus corrupt-schema repair, cached/fresh scan behavior, Claude-style metadata parsing, catalog health failures, root/entry/traversal-diagnostic/public-diagnostic/file/depth/encoding/total/actual-JSON bounds, public path redaction, stale SHA rejection, safe built-ins, process-allowlisted/missing/disallowed variables, env-file automatic eligibility/override/hot reread/malformed failure, one-pass and non-spoofable runtime-variable behavior, pre-allocation rejection of repeated runtime-placeholder expansion, exact final reply sizing at the positive boundary, allowlisted-environment backtick-fence amplification over the negative boundary, and isolated WebSocket/real-Chrome list/rescan/load operations with no command ledger or tmux side effects. |
| Every discovered Skill starts uninstalled and is tracked in fixed schema-v2 `skill-install-state.json` under `AI_CHAT_SHELL_STATE_DIR`. The management list shows `Install`, `Installing…`, `Installed`, retry, or no-installer state per Skill. Only a trusted user click plus native confirmation can request install. Exact id/Skill SHA/installer SHA/catalog validation, no-follow inode-safe reads, a private immutable snapshot, minimal environment, bounded output, a 600-second stdout/stderr idle deadline, single-flight execution, server-authenticated receipts, atomic 0600 JSON writes, and post-run freshness prevent stale, forged, or failed installs from becoming loadable. Either output stream resets the idle clock even after capture saturation; main-shell exit and idle kill have bounded descendant/pipe settlement. Long installs use a 20-second browser WebSocket heartbeat without an absolute client watchdog and bypass the short origin-scoped sync lock while server-side install serialization remains authoritative. Nonzero/signal/idle-timeout details are sanitized into one-use background memory and displayed only in an extension-origin result window; the chat content script receives only a token, and no diagnostic enters AI/composer/ledger/log/storage. New/deleted/changed Skills reconcile the JSON; SKILL.md or installer changes reset installed state; meaningful repairs/installed-set changes advance the version without an effective SHA. | `tests/server_skill_install.test.js`, `tests/server_skill_protocol.test.js`, `tests/background_skill_sync.test.js`, `tests/content_skill_helper.test.js`, `tests/content_compact_panel.test.js`, `tests/skill_install_result_page.test.js`, `tests/chrome_extension_e2e.test.js` | Covers default-uninstalled state, fixed JSON schema/file/key, installed-only AI catalog and load gate, strict exit-0 success, nonzero/null-code/signal/missing/stale Skill/stale installer/idle-timeout/change-during-run outcomes, stdout/stderr idle reset beyond the nominal interval, capture-saturated reset, bounded output tail, background descendants holding inherited pipes, immutable snapshot and TOCTOU resistance, minimal environment, idempotence and double-click/multi-tab single-flight, status progress while install response is pending, no absolute browser watchdog plus heartbeat, owner-tab/one-use popup tokens, ANSI/control/bidi sanitization, extension-page plain-text rendering, token expiry/rejection, JSON spoof rejection, add/delete/SKILL/installer/corrupt-state reconciliation, metadata-only stability, trusted/cancelled/synthetic clicks, close/deactivate recovery, server/transport/list failures with row-local Retry, no composer sync, and real-browser success plus exit-23 failure popup/Retry/uninstalled/composer-isolation behavior. |
| **View Skills is the complete local management inventory:** it renders exactly one row for every successfully discovered valid Skill, regardless of installation state or the AI-catalog serialized-metadata limit. `installed` is supplemental row state and never filters the management list; invalid Skill records appear only as diagnostics. Only a complete valid scan may reconcile persisted installation receipts and catalog state, so a malformed neighbor, transient missing root, or other partial scan is read-only and recovery restores the prior authenticated state without a false version change. A never-created implicit default root remains a healthy empty catalog, but a default root that previously persisted Skill identities fails closed while missing. An already-open dialog refreshes latest-only across tabs, coalesces concurrent polls, accepts a legitimate version reset after catalog-state repair, preserves rows with a visible error on refresh failure, yields to a local lifecycle that starts while its request is pending, and never writes to the chat composer. The AI-facing catalog remains independently fail-closed and installed-only. | `tests/server_skill_catalog.test.js`, `tests/server_skill_install.test.js`, `tests/content_skill_helper.test.js`, `tests/chrome_extension_e2e.test.js` | Covers mixed installed/uninstalled inventory equality, 500 discovered Skills beyond the AI metadata budget while retaining every management row and its installed status, installed-only empty AI response at that same boundary, malformed-neighbor plus explicit/default missing-root state-file byte stability and exact recovery, the never-created-default compatibility case, unchanged/single-flight/version-reset/stale/throw/resolved-failure/closed-dialog/pre-existing-and-late-starting-local-lifecycle refresh cases, and a real-Chrome two-tab install → uninstall → reinstall sequence with the second tab's open inventory updating each row without any composer or user-message mutation. |
| Skills synchronization is origin-scoped and latest-only across tabs. One tab owns a short-lived challenge; force creates a fresh challenge, removes only obsolete never-written Skill sync deliveries, durably marks an in-flight queued retry so reload can discard it if its attempt never settles, and may replace only the same owner tab's lost challenge; cumulative local changes replace the desired catalog, closed tabs leave no orphan lock, and stale/cross-tab/cross-origin acknowledgements are rejected. Only the owner tab receives its active challenge on state recovery. If a host exposes the final Skill list/ACK article only after generation controls and candidate-bound mutations are gone, baseline recovery is restricted to an exact active owner challenge in the final explicit assistant message after the latest explicit user turn; it never grants recovery to ordinary Skill loads, shell helpers, stale challenges, other tabs, or older turns. Once accepted, that page-scoped helper semantic is an inert tombstone across replacement DOM roots: host redraws cannot repeat list/ACK backend work or create another AI reply. The AI catalog contains only installed and loadable Skills. Every entry contains its complete id, name, description, and SHA; runtime instructions require the fixed memory entry to retain descriptions as routing metadata while refusing to follow instructions embedded in metadata. A healthy catalog requires acknowledgement even when it contains zero Skills. The AI must return the exact positive `catalog-version`; ACK validation and pending-delivery identity bind both raw aggregate SHA and version so installation-JSON changes with the same SHA cannot be acknowledged stale. Host layout-only line breaks may be repaired from canonical `textContent` only when rendered/raw forms are line-break-equivalent, each is one complete matching Skill envelope, rendered validation fails, and canonical validation succeeds. On M365, the current exact `.fai-CopilotMessage[role="article"]` root remains compatible with the legacy assistant class; its canonical Skill text may additionally repair the host's exact newline-to-one-space visual collapse only inside that explicitly authored assistant article and only when the whole raw node is one valid envelope. One terminal canonical layout newline is ignored only for that exact projection; a second newline or any other suffix still fails closed. M365's exact newline-free serialization of the fixed 12-line plugin-owned Skill sync prompt is presentation proof only for a fresh `.fai-UserMessage[role="article"]` root in the pending delivery lifecycle. Known plugin-owned structured payloads are projected to that newline-free form before their first M365 composer write, preventing Lexical from dropping standalone JSON-brace paragraphs while preserving every non-newline character; arbitrary text and pre-existing drafts are never projected. A restored strong composer displaces a stale automatically remembered weak input, while an explicit **Bind input** survives automatic events and reload until cleared. Automatic Skill execution is bound to one explicit current assistant message after the latest explicit user turn; an atomic complete response is eligible in its first observer batch, while unknown-role and older historical messages fail closed to **Process Skill**. A route-carried epoch is frozen to that exact response root plus unchanged user text and an append-only response prefix, survives slow completion beyond the former three-second tail, and is limited to one route. Historical semantics survive temporary empty redraws, so known or late-hydrated history remains inert. Every awaited backend and pre-presentation delivery step retains exact originating-root ownership. A queued result may cross one URL assignment only after the new thread settles and the same runtime root, semantic helper, transcript, and stored proof remain current; URL change, copied text, another chat, detach, same-URL replacement, helper→other→helper, a second route, or restored proof without runtime continuity fail closed before composer focus or any write. Live inserted and submitted-unconfirmed Skill deliveries may cross the one proven assignment only through the same exact volatile origin proof and exact plugin-owned composer text, resuming send-only without a second write; they lose all send and finalization authority on same-URL origin replacement, invalid or second routes, and a stale cancellation cannot clear a newer queue. Releasing the Skill single-flight lock always wakes the scanner so a second completed helper cannot be stranded. | `tests/background_skill_sync.test.js`, `tests/content_skill_helper.test.js`, `tests/content_skill_load_dispatch.test.js`, `tests/content_compact_panel.test.js`, `tests/chrome_extension_e2e.test.js` | Covers first-time empty-catalog synchronization, silent empty ACK, failure/retry, origin isolation, current/Force behavior, installed-only lists and full descriptions, version-only installed-set changes, deletion of the final installed Skill, dynamic green/current panel text, owner locking, owner-only challenge recovery with non-owner redaction, TTL/tab-close and close-during-begin recovery, same-owner force replacement, cleanup of obsolete queued and concurrently in-flight queued Skill prompt/list/error responses, durable mark→persist→reload compaction, inserted-phase one-write behavior, other-owner rejection, missing/malformed/wrong/stale SHA/version ACKs, semantic-key version separation, fixed-entry acknowledgement, a real-Chrome `catalog-version` value split by host layout, one exact background ACK, naked numeric-line and genuinely hidden-extra-content rejection, exact M365 Copilot class plus legacy-class role recognition, current-response generation and old-history/missing-role negatives, exact newline-collapse recovery with one terminal-newline positive plus two-newline/prose/second-envelope/whitespace negatives, fresh-root baseline-counted sync-prompt confirmation with suffix/different-challenge/history negatives and one finalization, exact owner-challenge late-final list recovery plus wrong-challenge/non-owner/later-user/load negatives, committed list/ACK same-semantic replacement-root tombstones with no second backend request/catalog/protocol-error reply, binding persistence/clear behavior, weak-input displacement, exact load/list parsing, queued error recovery, explicit response-root generation proof, atomic-complete positive dispatch, unknown-role and historical partial→complete negatives, removed/new/moved/reused Stop boundaries including same-route DOM-node reuse and permanently stale old-route controls, later-user epoch invalidation, slow exact-root route carry, new-root and same-Element rewritten-chat rejection, same-root cold redraw/live reuse, empty-settle late-history manual recovery, stale backend responses, same-URL replacement, exact retained-root queued recovery, cross-chat and second-route queued-result discard before focus/write, queued/inserted/submitted-unconfirmed same-URL await cancellation, stale-cancellation/new-queue isolation on route and same URL, in-flight scan wakeups, pending-agent deferral, latest-only manual actions, user/plugin-output rejection, M365 flattened provenance, exact structured-only pre-projection with ordinary-text/other-host/brace-loss negatives, a host-mapped real-Chrome M365 DOM scan and complete late-final flattened prompt → Copilot list → brace-preserving catalog → Copilot ACK chain without generation controls plus replacement-root redraws and first-UUID-route Skill load completion, panel states and controls, isolated-tab lifecycle cases, and unsafe-variable/duplicate-name/missing-file negatives. |
| Automatic executable/Skill dispatch requires current-turn proof bound to one explicit assistant message after the latest explicit user message. Same-batch complete and streamed responses may execute, but older history, unknown role, user output, plugin output, and late hydration stay inert with **Force run** or **Process Skill** recovery. Generation controls use exact known labels/stable test ids or the localhost-only structural fixture marker; a generic `Stop` button, a control rendered inside authored message content or the extension panel, and a removed-only control with no prior epoch are not evidence. A separately removed and re-added same-route host control may establish a genuinely new response epoch, while a control snapshotted from the old route stays stale for the whole new lifecycle and a later explicit user message immediately revokes the old response. One route may carry the exact response only while its user text remains unchanged and response content only appends; slow completion remains eligible, while a second route, an old/moved Stop, a different root, or same-Element transcript rewrite cannot authorize execution. A Skill backend result remains bound to the exact render generation; queued local Skill content crosses a route only after settled exact-root/transcript revalidation, never on URL change alone. | `tests/content_skill_load_dispatch.test.js`, `tests/content_last_shell_call_candidate.test.js`, `tests/chrome_extension_e2e.test.js` | Focused VM cases cover localized production stable-test-id acceptance, authored-message/panel stable-id rejection, generic-Stop rejection, atomic complete positive dispatch exactly once; partial→complete positive; historical two-stage and unrelated-generation negatives; unknown-role/manual recovery; new, retained, moved, removed, same-route reused, and old-route stale controls; removed-only cold evidence; later-user invalidation; expired-tail exact-root carry; post-route new-root and same-Element rewritten-chat rejection; second-route invalidation; backend detach/change; same-URL and restored-proof rejection; retained-root queued recovery; and cross-chat queued discard. Dedicated real-Chrome tabs cover atomic localized structural evidence, historical partial hydration, slow route carry, old-Stop/new-root rejection, exact retained-root pending recovery, unrelated-chat discard, cold/late history, and explicit user rejection. The real manual-page shell and agent buttons use the shared Stop→partial→complete fixture. |
| Extension release version, server protocol version, helper protocol version, Skill protocol version, and visual protocol metadata stay separate; core diagnostics remain concise when optional features are unused, while server health still exposes capability metadata and transport/script limits. | `tests/protocol_metadata.test.js`, `tests/background_write_file_message.test.js`, `tests/background_vision_message.test.js`, `tests/background_skill_sync.test.js`, `tests/popup_config.test.js`, `tests/manifest_consistency.test.js`, `tests/chrome_extension_e2e.test.js` | Covers `serverProtocolVersion = 12`, `helperProtocolVersion = 4`, `skillProtocolVersion = 6`, `visualProtocolVersion = 1`, release metadata and resource limits in `/health`, static real-Chrome preflight alignment, background rejection of old, missing-helper, helper-mismatched, or Skill-mismatched servers before forwarding, concise popup health text when optional features are unavailable, and source guards for floating-panel protocol status text. |
| Plain text shell helper parsing accepts `ai-helper-shell-start`, command body, and `ai-helper-shell-end`; every line between start and end is opaque command text, so heredocs and `cat <<EOF` file creation are preserved. Command text is never rejected merely for containing `shell-output`, result headings, Markdown fences, terminal prompts, metadata labels, or nested helper-marker text. AI-facing helper examples use four-backtick fences while rendered marker-only blocks remain supported, and closed four-backtick fenced shell helpers can recover from a missing `ai-helper-shell-end`. Shell bodies run from temporary script files and may exceed the former 8000-character limit up to a 1 MiB UTF-8 resource bound. | `tests/content_plain_text_shell_call.test.js`, `tests/tmux_helpers.test.js`, `tests/chrome_extension_e2e.test.js` | Covers parsing, extracted helper blocks inside surrounding text and four-backtick markdown examples, no-target shell helpers, legacy target-looking lines treated as command text, heredoc command preservation, opaque command validation in extension/server/board/vision, UTF-8 byte accounting, bounded command echo metadata, real keyword-heavy heredoc execution, real execution above 8000 characters, fenced missing-end recovery, matching payload hashes with explicit end markers, and non-recovery when either fence boundary is missing. |
| Plain text file helper parsing accepts `ai-helper-file-start`, filename, exact content, and `ai-helper-file-end`; closed four-backtick fenced file helpers can recover from a missing `ai-helper-file-end`. | `tests/content_plain_text_shell_call.test.js` | Covers JSON-like file content as plain text, trailing blank lines, file validation, file helper result formatting, and fenced missing-end recovery with trailing blank content preservation. |
| Draw.io helpers wrap the complete native `<mxfile>` body as non-executable data, independently select the last complete non-user candidate without relying on the optional author-role filter, and make that helper the sole current preview outcome. Rendering is offline through a pinned packaged viewer in an unprivileged sandbox iframe; changed valid XML stages in a transparent, noninteractive, viewport-intersecting layer and atomically replaces the current iframe only after SVG readiness. Parent startup and render acceptance use separate visible-page-time checks, iframe load recovers a lost ready message, and one fresh iframe retry absorbs a transient first-attempt acceptance timeout. A final pre-render acceptance timeout fails within its visible-time budget; after `render-started` is acknowledged before rendering begins, the parent deadline is cancelled and the isolated viewer applies its own 15-second visible-time SVG output check. Every latest selection, including an already-current artifact or cached invalid/render failure, cancels a different staging renderer before restoring its local outcome. Success clears old errors and remains silent. Validation or final renderer failure clears the old SVG/download, displays only the newest bounded preview/browser-console error, and sends one bounded report through the existing durable one-write composer queue without contacting backend/tmux. Render completion and error delivery remain bound to the exact originating candidate/turn through settings, local-state load/persist, and composer-attempt awaits; only one proven host-independent provisional-to-opaque-id assignment may carry that ownership, including ChatGPT and M365 UUID assignment. Incomplete streaming, stale/cross-chat, and superseded completion retain the prior confirmed outcome without flicker or writing into another conversation. | `tests/content_drawio_helper.test.js`, `tests/content_drawio_route_guard.test.js`, `tests/drawio_preview.test.js`, `tests/chrome_extension_e2e.test.js`, `tests/manifest_consistency.test.js`, `tests/package_release_version.test.js` | Covers exact body and identity parsing, explicit end marker plus CDATA collision handling, 1 MiB UTF-8 limit, `<mxfile>`/`<diagram>` validation, non-runnable classification, last-complete rather than last-valid selection, role-filter independence with explicit-user rejection, local viewer provenance/license/attribution and no-CDN source guard, sandbox/CSP/WAR alignment, release-archive inclusion, transparent in-viewport staging that remains layout-active under Chromium cross-origin frame throttling while staying noninteractive and accessibility-hidden, fresh message channel, listener-before-navigation ordering, ready/load handoff, hidden-page watchdog pause/resume, phase cancellation, first-attempt acceptance retry followed by bounded final pre-render failure, no parent deadline after `render-started`, behavior-tested source/channel/artifact/phase rejection, VM-executed synchronous/asynchronous/missing-SVG/throw outcomes with observer/timer/listener cleanup, failure clearing, latest-only error replacement, browser-console diagnostics, Close/Reopen/download controls, browser-viewport Maximize/Restore, one renderer mount for two rapid helpers, zero mounts for incomplete streaming, successful replacement and error clearing, A→B→A current-artifact supersession plus cached-invalid→B→cached-invalid restoration without duplicate error count, malformed/renderer failure one-time AI replies, success silence, same-artifact redraw idempotency, settings/persistence race guards on same-page, transcript-replacement, second-permanent-route, and exact retained provisional-route cases, render-completion route races, repeated Draw.io-only real-Chrome runs, and real-browser proof of no shell-backend side effects. |
| Plain text board helper parsing accepts `ai-helper-board-start`, exactly one opaque board command line, and `ai-helper-board-end`; named markers such as `ai-helper-board-R1-start` route to `board-R1`; closed four-backtick fenced board helpers can recover from a missing end marker. | `tests/content_plain_text_shell_call.test.js`, `tests/tmux_helpers.test.js` | Covers default and named board parsing, identity suffixes, semantic key differences, named end-marker matching, fenced missing-end recovery, empty command rejection, multiline rejection, size bounds, and keyword/helper-marker text preservation. |
| Plain text agent-message helper parsing accepts `ai-helper-agent-message-start`, `to` and optional `task-id` headers, a blank-line-separated body, and `ai-helper-agent-message-end`; closed four-backtick fenced agent-message helpers can recover from a missing end marker. | `tests/content_plain_text_shell_call.test.js` | Covers agent-message parsing, identity suffixes, semantic key differences, safe recipient validation, task-id validation, empty body rejection, fenced missing-end recovery, helper detection hints, slave reply prompt formatting with non-executable quoted helper markers, default master agent ID suggestions, and roster summary formatting. |
| AI master pages can autonomously discover available slave agents and inspect delegated task state through read-only `ai-helper-agent-roster-*` and `ai-helper-agent-task-status-*` helpers. | `tests/content_plain_text_shell_call.test.js`, `tests/content_agent_delivery.test.js`, `tests/content_last_shell_call_candidate.test.js`, `tests/agent_hub.test.js`, `tests/background_agent_message.test.js`, `tests/chrome_extension_e2e.test.js` | Covers roster/status helper parsing, filters, validation, candidate detection hints, rejection of malformed query helpers, repeatable query behavior, suppression of query helpers embedded inside `shell-output`, background forwarding of `agent-list` and `agent-task-status`, server `agent-list` capability metadata including `surface`, `replyMode`, `pendingCount`, `canReceiveTask`, `capabilities`, `lastSeenAgeMs`, `stale`, and `staleReason`, content dispatch from helper to hub query, formatted `shell-output` for roster and task status without executable follow-up helper blocks, roster next actions that only recommend slaves with `canReceiveTask=true`, browser e2e proof that roster/status helpers work from a registered master tab, and AI-oriented recovery hints for missing recipients or stale tmux-ai panes. |
| JSON helper requests and old `ai-helper-start-shell` aliases remain unsupported as outer helper envelopes, while the same text remains valid as opaque content inside a supported command body. | `tests/content_plain_text_shell_call.test.js` | Covers language-hint/parser rejection for unsupported outer envelopes without reintroducing command-body keyword filtering. |
| Optional helper identity suffixes distinguish request payload identities for parsing and diagnostics, but do not control server execution dedup. | `tests/content_plain_text_shell_call.test.js`, `tests/chrome_extension_e2e.test.js` | Covers suffixed shell and file parsing, semantic key differences, malformed suffix rejection, unsuffixed payload hashes, removal of frontend shell-output command dedup, and a real suffixed shell e2e request. |
| The content script scan-debounces the exact rendered helper request and never treats command text, a prior semantic key, cancellation/failure, or historical `shell-output` as execution proof. Once a backend operation has returned, composer failure cannot re-execute that rendered helper. Every extension-owned result, including backend failure and rejected-helper feedback, uses a durable one-write delivery queue; while the complete plugin-owned composer text remains unchanged, the outer queue may make bounded send-only retries in the same page lifecycle without repeating the backend operation or composer write. One v0.8.9 actuator invocation still performs its original bounded 80-round send-readiness loop with button, form, and keyboard fallbacks; a bounded background delay message preserves its 125/150ms timing in hidden tabs. Any composer containing user characters before the write is user-owned, including whitespace-only or exact pending text; M365's real empty `<p><br></p>` shape is not. User removal/replacement or a competing reply draft revokes send authority and cancels the already-queued output batch without reinsertion or presentation receipt, while a framework redraw preserving exact plugin-written text may receive one bounded in-actuator node handoff after a short read-only empty-node settle. A cleared composer enters submitted-but-unconfirmed observation rather than immediate cancellation; neither clearing, an auto-send-disabled insertion, nor the pinned actuator's `sent` heuristic can create a tombstone or presentation receipt without a fresh exact submitted-message root. Historical identical roots captured before delivery, including a virtualized root that is later remounted, are not fresh proof. The guard keeps exact ownership on its actuator node, rejects every visible non-empty strong reply draft regardless of focus or candidate score, treats an actively edited ambiguous textarea fail-closed, ignores inactive generic tool/side-panel textareas, detached/hidden nodes, detached subtrees and trusted user mutation of the owned/current reply composer, and terminates the old loop at its next await boundary after ownership loss. Exact redraw copies, unrelated search/code inputs, and the extension panel's own inputs are never treated as competing or remembered chat composers. Each actuator start is recorded synchronously after final guard preflight without awaiting persistence before its first original actuator call; a preflight veto remains retryable without consuming a send attempt. A same-tab provisional-to-opaque-id route transition preserves queued, inserted, or submitted-unconfirmed ownership only for a live helper entry whose volatile exact candidate/turn guard successfully rebases; restored ordinary entries fail closed even across an apparent ChatGPT provisional assignment. A route with an exact pending handoff recognizes the prior rendered-helper key so the same DOM helper cannot execute again solely because its URL changed; new routes without pending ownership and recycled/new render roots remain eligible. The retry timer observes route-only URL changes before storage selection. Submitted/presented receipt state and local presentation tombstones may survive solely for receipt cleanup, without composer or old-chat status side effects. The panel distinguishes backend completion, model submission pending, and server receipt pending; its own DOM mutations and a benign scan of the already-handled source helper cannot replace terminal completion. On `m365.cloud.microsoft`, post-write ownership alone may accept the exact Lexical transformation that removes line breaks and appends its `U+200B U+200C` caret sentinel. Fresh exact `.fai-UserMessage[role="article"]` roots prove ordinary submissions; because M365 irreversibly serializes extension-inserted known structured deliveries (fenced `shell-output` or identity-bearing agent prompts) as one newline-free text node, their strict no-trim host-serialization equivalence is accepted only inside the plugin-owned delivery lifecycle. | `tests/content_plain_text_shell_call.test.js`, `tests/content_last_shell_call_candidate.test.js`, `tests/content_force_run.test.js`, `tests/content_route_delivery.test.js`, `tests/content_send_actuator.test.js`, `tests/background_write_file_message.test.js`, `tests/chrome_extension_e2e.test.js` | Covers historical output followed by a new helper, virtualized replacement and same-Element recycling, identical helpers in a shared container, pending route-key carry without global frontend command dedup, live-proof queued file-result route carry with one backend request, restored queued/inserted/submitted-unconfirmed shell and Draw.io route rejection for both permanent-chat navigation and apparent provisional assignment, receipt-only restored submitted/presented cleanup with no composer/send/status effect, SPA exact-text route-only ownership transfer without DOM mutation, lifecycle invalidation, execution-lock release, response-loss recovery without same-render replay, one-write normal/failure/rejected-result delivery with bounded same-lifecycle send-only recovery, a single bounded exact-text redraw handoff, an empty-first delayed composer restoration, delayed exact submitted-message proof across a later retry after a host-cleared composer, cleared-without-submission never reporting completion or emitting a receipt, auto-send-disabled manual-proof waiting, M365 newline-flattened post-write ownership without changing the pinned send actuator, the real empty Lexical DOM shape, strict rejection of every pre-existing non-empty draft plus malformed/wrong-host composer signatures and leading/trailing whitespace changes, explicit generic-vs-structured M365 newline-equivalence scope, M365 user-vs-Copilot submission roots, baseline-counted identical history and historical-root remount rejection, exactly one new presentation receipt, document and detached-subtree stale-event blocking, dual-connected-visible composer draft rejection including a lower-scored inactive strong reply composer without consuming actuator generation, inactive generic tool-textarea tolerance with active ambiguous-textarea fail-closed behavior, preflight-veto retry state without false completion, dual-connected exact redraw acceptance, old-loop cancellation after a detached reparent, trusted deletion during the v0.8.9 confirmation window, unrelated search mutation tolerance, extension-panel/search composer exclusion, synchronous guard-to-first-actuator generation timing even when storage persistence never resolves, background handler timing plus a real-browser hidden-tab full submission, route-safe presentation receipt/tombstone recovery, intentional deletion cancellation, exact-text composer redraw reacquisition, unrelated user-draft rejection, connected-but-hidden stale composer rejection before actuator invocation, React-style tracked textarea state, terminal panel-status preservation across benign handled-helper rescans, full-SHA-256 single-declaration v0.8.6/v0.8.9 send-actuator source locking, non-shell side-effect safety, persistence, strict ownership normalization, Force run, and real Chromium exact full-text submission after an empty-first delayed composer replacement followed by an SPA URL change. |
| A fresh exact submitted-message root wakes a pending inserted or submitted-unconfirmed delivery directly from the page MutationObserver, before the ordinary two-second retry or a route/reload can erase the proof. Non-matching roots, no proof, and remounting the same historical root do not finalize or emit a presentation receipt. Route handoff preserves submitted-unconfirmed ownership: exact plugin text may resume send-only without a backend replay or rewrite, while a non-exact non-empty user draft cancels the pending batch and is never sent. | `tests/content_route_delivery.test.js` | Covers immediate observer-driven finalization, non-matching/no-proof/historical-remount negatives, exact submitted-unconfirmed route recovery with backend/write counts pinned to one, and user-draft/non-exact-text route cancellation. The route test harness prints each completed case and has a native watchdog so an unresolved VM promise cannot exit falsely green. |
| A new chat may receive its permanent SPA URL before the first response's helper finishes rendering, while the scanner is awaiting pending state or outer enabled-host settings, while that helper is awaiting execution settings or an agent-profile lookup, while its backend request is in flight, while a lost MV3 runtime channel is recovering that exact shell/board attempt by read-only status, or while its result is entering the persistent delivery queue. The scanner reconciles the current route lifecycle after every pre-candidate storage await, so candidate page identity and generation cannot come from opposite sides of navigation. Before backend ownership begins, one exact-token claim covers execution settings, validation/chain-limit rejection, and cached-pending lookup; a same-turn stable-ID DOM redraw cannot start a second scan, an old continuation cannot clear a newer claim, and Force cannot bypass it. A trusted Force or Process Skill action owns manual priority across pre-candidate awaits, so an overlapping automatic scan defers instead of dispatching the same helper a second time. After active ownership begins, every asynchronous profile lookup is followed by a runtime ownership guard immediately before shell, board, file, agent-send, roster, or task-status dispatch: the call token records runtime dispatch synchronously beside `sendMessage`, so a pre-runtime route releases the old claim for one settled automatic rescan while an already-dispatched operation can only recover its original execution. Automatic calls dynamically validate their exact originating turn, while Force calls require the same exact active call token and page generation. Direct legacy helper-function callers that provide neither ownership object retain their existing behavior. The exact retained originating transcript/helper may carry that one automatic operation into the assigned route, enqueue it durably, write once, submit once, and record one presentation receipt without replaying the backend. Skill backend ownership may cross only one exact host-independent provisional-to-opaque-id assignment, including M365 `/chat` to `/chat/<UUID>`; a retained root on one permanent conversation URL moving to another remains stale. Pending creation revalidates its lifecycle after both state loading and durable persistence; a stale continuation removes and persists only its exact entry object, never a newer same-call replacement. If the route replaces or removes the originating transcript/helper, or an already-permanent conversation changes route, the old helper/result must produce no new backend, rejection reply, cached delivery, composer, pending-delivery, or submitted-message side effect beyond work already proven to have started; recovery revalidates ownership before and after every awaited preflight/status step. | `tests/content_last_shell_call_candidate.test.js`, `tests/content_force_run.test.js`, `tests/chrome_extension_e2e.test.js` | Focused gated-outer-settings, gated-execution-settings, deferred-profile, deferred-backend, cached-pending, automatic rejection, persistence, and runtime-channel-loss scan regressions cross the actual content-script lifecycle: retained `/` to `/c/...` Skill load and shell dispatch reconcile the post-await generation and reply exactly once, while transcript replacement and `/c/a` to `/c/b` dispatch zero times; a concurrent second scan over the stable-ID redrawn turn still produces one backend/pending/write/send/receipt chain; a rejected settings read releases its precise claim so the same semantic helper in a new DOM root retries once; a changed helper may take a newer claim that the stale continuation cannot clear; trusted Force remains blocked by a live claim; auto-first and manual-first races give Force and Process Skill exactly one backend/reply with manual priority; retained validation and chain-limit rejections reply once while replacement replies zero times; Force validation rejection cannot cross either its auto-send-settings or pending-persistence await; cached pending lookup cannot write after transcript replacement or `/c/a` to `/c/b`; deferred profile lookup permits each automatic shell, board, agent-send, roster, and task-status exactly once across a retained `/` to `/c|/uc` turn, but sends each zero times after transcript replacement or `/c/a` to `/c/b`; the same five Force helper kinds send once on the same lifecycle and zero times after replacement or `/c/a` to `/c/b` while profile lookup is pending; a gated Skill backend response queues once across `/` to `/c|/uc` but never across `/c/a` to `/c/b`; shell and board channel loss recover only through their kind-scoped status endpoint and then write/submit/receipt exactly once; replacement during execution-settings wait dispatches zero times; retained `/c/a` to `/c/b` dispatches zero times; transcript replacement before recovery or a second route during an in-flight status query stops recovery without another `run`, write, or queue; a deferred pending-state write preserves a retained exact route once, but transcript replacement cleans its exact old entry without starting delivery or overwriting a newer active status; a newer same-call entry survives old-continuation cleanup; Force backend and validation results retain their strict original route boundary; retained backend completion writes/submits/receipts once. Dedicated one-case-per-page real-Chrome tests use the real ChatGPT and M365 authored-turn contracts to prove a helper whose final DOM mutation occurs after route assignment, delayed real tmux commands delivered across in-flight retained transcripts, M365 Lexical post-write composer redraw with send-only recovery, M365 Skill load after UUID assignment, transcript-replacement rejection, and permanent UUID A-to-B rejection, each with exact one-execution marker files where applicable. |
| A recognized provisional-to-opaque-id assignment is quarantined until the new route is scan-settled and has remained stable for at least four seconds. During that window no automatic backend start, Skill request, Draw.io commit, composer write, or send may use a temporarily retained old transcript. Exact live turn proof is rechecked after settlement; a result already written before the route can retain only send authority, never another write. | `tests/content_last_shell_call_candidate.test.js`, `tests/content_skill_load_dispatch.test.js`, `tests/content_drawio_route_guard.test.js`, `tests/chrome_extension_e2e.test.js` | Unit tests cover delayed transcript replacement before the quarantine expires, unchanged-user and changed-user carried-generation boundaries, no pre-runtime dispatch before settlement, and exact retained rescan after settlement. Independent real-Chrome M365 pages keep an old transcript mounted until its real tmux command has finished, replace it before the four-second boundary, and prove zero transient submissions, one runtime request/marker execution, no composer leak, and eventual zero pending entries. A second case replaces transcript and composer after the first plugin write and proves one write, zero submissions/reinsertions, one backend request, and zero pending entries. Skill cases prove one request/reply for retained exact roots and zero requests/replies for a delayed replacement turn. Focused races additionally cover validation and chain-limit error queuing plus successful ACK refresh across an automatic retained assignment, with replacement, permanent A-to-B navigation, and manual Process/Force paths all remaining strict and producing zero migrated queue/state effects. |
| Shell-output formatting is compact while preserving command/execution identity, interruption, clean recovery, retryability, and per-pane queue metadata. Authoritative duplicate-control responses format to an empty string as a defense in depth and never enter model-facing output. | `tests/content_shell_output_format.test.js`, `tests/content_last_shell_call_candidate.test.js` | Covers short, long, and multiline commands, `cmdHash`, `executionId`, timeout/process state, Ctrl+C, queue metadata, clean `recovered: true` output, local-only already-presented duplicates, and removal of `duplicate`, `skipped`, replay, reason, and previous-call diagnostics from an unpresented stored-result recovery. |
| Browser/background ledgers are non-blocking audit records, while the shell server is the sole duplicate authority after resolving the actual tmux pane. Only a prior server-proved execution with the same tmux server/pane + current pane-shell PID + command + actual-cwd fingerprint can be skipped; Ctrl+C after execution starts is history, while pre-execution interruption, running, failed, timed-out/unconfirmed, unavailable-target, generic-board, different/recreated/respawned-pane-shell, incomplete-identity, and unscoped audit calls never become authority. A canonical execution identity survives duplicate chains; per-tab/page presentation receipts remain durably pending until acknowledged, canonical pending deliveries coalesce by `executionId`, and a bounded local presentation tombstone suppresses stale unpresented replay responses after local delivery. Already-presented duplicates stay local-only, while a genuinely unpresented bounded result is recovered cleanly without duplicate metadata. | `tests/background_write_file_message.test.js`, `tests/background_vision_message.test.js`, `tests/server_dedup_force.test.js`, `tests/server_result_recovery.test.js`, `tests/content_last_shell_call_candidate.test.js`, `tests/tmux_helpers.test.js`, `tests/tmux_forai_setup_integration.test.js`, `tests/tmux_shell_lifecycle_integration.test.js`, `tests/tmux_board_integration.test.js`, `tests/tmux_visual_run_integration.test.js`, `tests/chrome_extension_e2e.test.js` | Covers immutable attempt and canonical execution identities, terminal monotonicity, interrupted completion, same-pane duplicates, presentation receipts created before and after persisted duplicate attempts, failed receipt persistence/retry without composer reinsertion, same-`executionId` local pending coalescing, reload-persistent local presentation suppression, local-only delivered duplicates, clean unpresented replay, replay expiry, nonterminal retention, kind-scoped read-only status across fresh server load, simulated old-server response loss while a restarted module adopts the persistent pane owner, different cwd/pane/recreated pane, real `respawn-pane -k` shell replacement with a stable pane id, missing pane-shell metadata, board fail-open, Force bypass, and browser-to-server final adjudication without internal duplicate feedback to the model. |
| Force call (`callMeta.force=true` or top-level `force=true`) records `forced: true` and explicitly bypasses server-confirmed completed execution dedup. | `tests/background_write_file_message.test.js`, `tests/server_dedup_force.test.js`, `tests/tmux_visual_run_integration.test.js` | Covers force metadata recording, payload forwarding, top-level force consistency, ledger `forced` fields, and actual rerun after a completed same-pane execution. |
| Manual `Force run` is a generic recovery dispatcher for the latest eligible executable or Skill helper. Executable helpers preserve pending forced retry behavior, bypass authoritative server dedup only after an explicit trusted browser click, and may execute a helper whose exact fenced/DOM `shell-output` provenance prevented automatic execution; Skill helpers re-enter only the validated list/load/ACK protocol. Host-page `.click()` and synthetic events fail closed before dispatch. Candidate dispatch is page/lifecycle and semantic-payload bound so an equivalent redraw does not invalidate the click, while a changed payload, later helper kind, route change, or active operation fails closed. Composer delivery, agent drafts, and assistant DOM activity cannot veto a trusted user click. Command-body keywords never veto Force in either extension or server. | `tests/content_force_run.test.js`, `tests/content_force_run_idle.test.js`, `tests/content_last_shell_call_candidate.test.js`, `tests/content_skill_load_dispatch.test.js`, `tests/content_plain_text_shell_call.test.js`, `tests/tmux_helpers.test.js`, `tests/chrome_extension_e2e.test.js` | Source guards plus fake-clock/runtime cases cover trusted/synthetic event gates, executable-vs-Skill routing, stable semantic redraw identity, changed-payload and lifecycle reset, operation pause/resume, non-operation churn, the exact 19,999/20,000 ms boundary, no-candidate clearing, active-call pending retention, stale route/candidate rejection, newest-helper selection, mixed quoted output, and explicit structurally suppressed execution. A dedicated real-Chrome page replaces the helper DOM throughout the full idle interval, proves synthetic click and automatic execution both fail, then uses trusted CDP mouse input and observes the result. |
| Floating panel defaults to a compact, state-driven width: normal idle shows concise status, an always-visible saved-role badge (`None`, `Master`, or `Slave`), the local Skills version/update chip, and More. The Skills chip remains a distinct header control and is never replaced by Force run. A detected executable or Skill helper starts one 20-second accumulated idle clock keyed by page lifecycle, helper kind, and semantic payload; repeated scans, equivalent DOM redraws, assistant generation, and composer/agent delivery state do not reset or pause it, while a real helper/backend operation pauses and later resumes it. At the deadline the compact row exposes generic Force run; an actionable Skill may expose **Process Skill** before that deadline. Both recovery actions require trusted browser input. More → Setup & recovery always contains a Force run entry, disabled with no eligible helper or during an active operation. Errors add Server Check and active shell execution exposes Stop helper. The acknowledged gray Skills chip opens the local catalog without synchronization or composer output, the green update chip starts synchronization, invalid opens local diagnostics, and syncing remains disabled. Every prior setup/recovery, Agent/tmux-ai, Skills, full-status/debug, and page-binding control remains under five labelled advanced groups; Page binding is last/default-closed. Draw.io stays contextual. Output-idle state places Continue waiting and the exact-owner Stop helper together while hiding the redundant common Stop control. The exact idle panel colors remain unchanged; real helper work uses low-saturation blue, user-visible completion uses green, and failures/actionable exceptions use red while dot, text, and ARIA preserve non-color meaning. Successful backend results awaiting submitted-message proof remain blue, user-visible success stays green when only a server receipt is pending, and failed pending results are immediately red. Deferred completion, receipt, discard, cancellation, stale Skill, and progress continuations cannot overwrite a newer shell, Skill, recovery, or Force owner; the exact current operation can still publish its own terminal state. Stale Draw.io completions likewise cannot override a newer helper while exact current-artifact restoration completes green. | `tests/content_compact_panel.test.js`, `tests/content_panel_state_theme.test.js`, `tests/content_force_run_idle.test.js`, `tests/content_last_shell_call_candidate.test.js`, `tests/content_skill_load_dispatch.test.js`, `tests/content_skill_helper.test.js`, `tests/chrome_extension_e2e.test.js` | Guards the 292px layout, exact semantic backgrounds/borders, fallback-to-idle behavior, non-color ARIA labels, unchanged child controls, successful queued/inserted/submitted-unconfirmed versus submitted/presented/server-receipt-pending boundaries, failed pending delivery, Skill dispatch states, stable/cancelled/restored/latest-only Draw.io state, cross-helper status ownership including exact own-operation completion and deferred old receipt/discard/cancellation versus new shell/Skill/Force owners, call-key-scoped progress after profile awaits, manual invalid-Skill terminal status, stable header Skills chip placement, saved-role labels/colors/ARIA, trusted/synthetic recovery input, 20-second wall-clock semantics, pause/resume boundaries, redraw/churn resistance, generic executable/Skill dispatch, permanent advanced Force location/enabled state, immediate Process Skill-to-delayed Force transition, no-candidate/active-operation negatives, default-collapsed advanced area, last/default-closed Page binding, all prior controls, contextual Draw.io, output-idle Continue/Stop, Ctrl+C completion, and real-browser computed-style proof across idle/running/success/error. |
| README displays the current extension panel in acknowledged healthy idle, green Skills-update, Force run, blue running Stop, blue output-idle Continue/Stop, green completion, red failure, contextual Draw.io, expanded advanced controls, and bottom collapsed Page binding states using tightly cropped real-Chrome images. The unchanged idle/Skills views retain their valid v0.11.3 captures; helper-state and other exercised views are regenerated for v0.11.13. | `tests/readme_panel_screenshots.test.js`, `tests/chrome_extension_e2e.test.js` | Verifies every documented mixed-version PNG exists, has a valid PNG signature and bounded panel-crop dimensions, remains linked before the historical helper screenshots, and is generated by the screenshot-stage path in the real unpacked-extension E2E. |
| Floating panel "detected helper" debug view always reflects the most recent helper block, including while the chat thread is still streaming or before the quiet-period gate fires, and reports baseline/generation/Skill dispatch gate state separately from parse success. | `tests/content_last_shell_call_candidate.test.js`, `tests/content_skill_load_dispatch.test.js`, `tests/chrome_extension_e2e.test.js` | Covers the post-scan debug update, live-generation evidence, settled history-gate refresh, and a streaming-phase regression that asserts the panel shows the newest helper cmd and not an earlier one when scan early-returns due to thread text changes. |
| Popup settings UI can load, save, normalize enabled sites, list tmux targets, display default `ForAI` host/board/cwd state, and export/import portable config without ledgers. | `tests/popup_config.test.js` | Covers tmux target display, default workspace summary, site normalization, config export/import, clamping, and exclusion of command ledgers. |
| Tmux target parsing and resolution accept pane id, `session:window.pane`, and unique window names while rejecting ambiguous names. | `tests/tmux_helpers.test.js` | Covers pane parsing, target resolution, tmux socket argument building, and tmux environment socket extraction. |
| Board helper target selection, prompt extraction, normalized pipe-log capture, and examples stay stable. Delivery uses the immutable-pane queue and persistent lease, reads a stable prompt without injecting a probe Enter, submits literal text plus Enter atomically, and retains ownership after crossing the response timeout. Runtime loss recovers through kind-scoped read-only status and never resends the same rendered board helper. Shell-backed panes add process-group proof; generic TUI prompt evidence stays non-authoritative for dedup. | `tests/tmux_helpers.test.js`, `tests/tmux_board_integration.test.js`, `tests/server_result_recovery.test.js`, `tests/background_write_file_message.test.js`, `tests/content_last_shell_call_candidate.test.js` | Covers default/named/configured targets, errors, examples, validation, prompt capture, truncation, busy rejection, long follower serialization, prompt spoof protection, generic-TUI limitations, server-restart persistent-owner result reconstruction, completed bounded board status recovery with empty execution key, channel-loss polling without another `run-board`, and explicit repeated board execution. |
| Board helper tmux integration probes the prompt before sending commands and refuses promptless panes. Shell-backed boards combine prompt and process-group evidence; a generic non-shell TUI has no authoritative completion primitive, so prompt text is neither a strong serialization guarantee nor duplicate proof, and board execution dedup always fails open. | `tests/tmux_board_integration.test.js` | Uses a private tmux socket with fake `board` and `board-R1` panes to cover prompt-based capture, repeated same command execution, cwd changes, timed-out retry, shell-backed prompt-spoof output before actual completion, the documented generic-TUI limitation, unavailable targets, and probe failure without sending the command. |
| Page startup and tmux target listing ensure a default `ForAI` tmux session with `host` and `board` windows; new default windows use the configured cwd; reset recreates only the default `ForAI` session; shell helpers always run in `ForAI:host`, and board helpers default to `ForAI:board` unless a named board marker selects `ForAI:board-<suffix>`. | `tests/tmux_forai_setup_integration.test.js`, `tests/tmux_helpers.test.js`, `tests/content_plain_text_shell_call.test.js`, `tests/popup_config.test.js`, `tests/manifest_consistency.test.js`, `tests/feature_test_matrix.test.js` | Uses a private tmux socket to cover automatic session/window creation, realpath cwd policy, idempotent setup, reset with and without an existing session, default shell execution, heredoc file creation followed by later commands in one run, ignoring legacy shell target fields, default and named board target resolution, parser support for targetless shell helpers, popup default summary, content/background reset wiring, exact tmux session targeting so `ForAI` does not match `ForAI-*`, and docs/agent-guide protection against reintroducing the obsolete shell target line. |
| Agent pages route shell and board helpers, startup/health checks, and full-chain tests to an isolated per-agent tmux workspace while Role=None pages continue to use the default `ForAI` workspace. Floating-panel reset recreates only the exact current-role session; execution dedup remains scoped to the resolved pane instance. | `tests/background_agent_message.test.js`, `tests/background_write_file_message.test.js`, `tests/content_agent_delivery.test.js`, `tests/tmux_forai_setup_integration.test.js`, `tests/chrome_extension_e2e.test.js` | Covers `agentId: slave-a` mapping to `ForAI-slave-a:host`, board and tmux-control `agentId` forwarding, exact tmux target handling with default and per-agent sessions present, the same command executing once in each distinct pane, default reset/recreation, agent-only reset preserving the default pane, and real Chromium proof that, after the content script's explicit history-baseline scan, a tab still executes in and resets only `ForAI-slave-a` after page session storage is cleared and the page reloads. |
| Runtime state directory health is required before shell server startup and command execution; project `.state` is the default runtime state directory, safe conflicts are auto-repaired with `.broken-*` backups, `AI_CHAT_SHELL_STATE_DIR` remains an explicit override, and macOS LaunchAgent auto-start stays removed. | `tests/state_dir.test.js`, `tests/manifest_consistency.test.js`, `scripts/doctor.sh`, `scripts/test_all.sh` | Covers default state path selection, missing state auto-creation, state path as a file auto-repair, required subpath conflict auto-repair, corrupted ledger backup and recovery, state-log path repair, installer compatibility cleanup plus foreground launch, unwritable state where supported, health fields, content/background health wiring, and shell syntax checks for startup/cleanup scripts and state-log handling. |
| Server command claims are not left stuck as `running` when post-claim execution throws. A request waiting for a pane first receives a persistent, non-authoritative queued reservation; the initial persistent pane-owner claim is bound to that immutable attempt before authoritative queue-head adjudication, nonterminal attempts survive ledger count pruning, and late cleanup cannot downgrade terminal completion. | `tests/server_dedup_force.test.js`, `tests/server_result_recovery.test.js`, `tests/tmux_shell_lifecycle_integration.test.js` | Covers post-claim failure cleanup, initial owner binding to the reservation, same-process fresh-module restart recovery of the persistent lease while the original handler continues, release-CAS failure preservation and later recovery of completion proof, queued `run-status` visibility with empty pre-adjudication cwd/execution key, same-attempt queue-head adjudication, retention through terminal-entry churn, completed-state monotonicity, duplicate replay completion of a reservation, leader/follower execution, and an identical failed retry returning `run`. |
| Doctor diagnostics check Node.js, tmux, default `ForAI` host/board visibility, manifest ID, server origin policy, server protocol version, helper protocol version, state directory health, and default cwd validity without mutating tmux state. | `tests/protocol_metadata.test.js`, `scripts/doctor.sh`, `scripts/test_all.sh` | Automated coverage keeps doctor protocol expectations aligned with server/background metadata; shell syntax checks cover the script, while manual doctor runs exercise live tmux/server state and environment overrides. |
| Internal direct tmux visual-run test mode shares the persistent per-pane shell queue without sending a preemptive Ctrl+C, clears pane history only after acquiring ownership, executes one command through `/bin/sh -c`, marks completion via tmux window name, and dumps the tmux buffer only when `AI_CHAT_SHELL_ENABLE_DIRECT_VISUAL_TMUX=1` is set. | `tests/tmux_visual_run_integration.test.js` | Uses a private tmux socket to cover default production rejection, explicit test-mode enablement, shell-helper serialization/non-preemption, atomic literal+Enter, recovery of stale pre-dispatch ownership without executing buffered text, failed-ledger cleanup when execution/completion proof is missing, `&&`/`||` compatibility, nonzero exit propagation, stale history removal, done-window marker detection, capture-pane output, completed same-pane duplicate adjudication, timed-out execution retry, Force run bypass, and single-line validation. |
| Default tmux target failures produce a helper-readable failure without requiring AI helper blocks to name a target. | `tests/tmux_helpers.test.js` | Covers default target failure response shape and example helper block. |
| File helpers write only a single file name directly under `AI_HELPER_FILE_PATH` when that environment variable exists, otherwise under `$HOME/Downloads`; an explicitly empty override fails closed. The unified `docs/AI_INSTRUCTIONS_FULL.md` is directly pasteable Custom Instructions rather than a menu of variants. | `tests/tmux_helpers.test.js`, `tests/chrome_extension_e2e.test.js`, `tests/feature_test_matrix.test.js` | Covers default and overridden destination resolution, empty-override rejection, traversal rejection, byte counts, actual writes through the environment-selected default, and real Chromium page-to-content-to-background-to-WebSocket-to-server cases before and after restarting the managed server with `AI_HELPER_FILE_PATH`. Also guards every supported helper marker in the unified instructions and the absence of Minimal/Recommended/role-version template branches. |
| Tmux shell execution uses pid/status-aware foreground waiting plus a tokenized ownership lease persisted on each pane. The configured timeout is an output-idle interval: observable output resets it, expiry persists `awaiting-user` and emits a progress event without inventing a result, Continue starts another interval, and Stop helper revalidates the current role's exact owner and exact waiting execution when known before signaling only its foreground process group. Refreshed/restarted helpers recover status without resending, queued work does not consume idle time, different panes remain concurrent, and Ctrl+C before or after the executed marker preserves the correct retry/completion verdict. | `tests/tmux_helpers.test.js`, `tests/tmux_forai_setup_integration.test.js`, `tests/tmux_shell_lifecycle_integration.test.js`, `tests/tmux_visual_run_integration.test.js`, `tests/content_shell_output_format.test.js`, `tests/background_write_file_message.test.js`, `tests/server_result_recovery.test.js`, `tests/chrome_extension_e2e.test.js` | Covers output heartbeat reset, idle progress, repeated Continue, default-vs-agent role isolation, panel termination, stable socket + server-pid + pane-id queue keys, address changes, server-restart owner adoption, respawned-shell dedup isolation, missing process metadata, queue-head cwd, runner state files and signals, long-command follower queues, status recovery without resending, real bounded-latency interruption, and timeout/process/interruption metadata. |
| macOS UI-window vision control exposes structured health, local Terminal/Ghostty visual tmux candidate discovery, and high-level visual run requests by default; generic window listing/capture/type/key, message-level OCR, and Terminal self-test primitives require `AI_CHAT_SHELL_ENABLE_LOW_LEVEL_VISION=1` for local manual tests. | `tests/vision_messages.test.js`, `tests/background_vision_message.test.js` | Uses a fake macOS vision helper to cover low-level default rejection, low-level explicit test-mode validation, helper missing, non-macOS, permission denied, invalid window, local visual candidate filtering for Terminal.app/Ghostty, unsupported app rejection, app mismatch rejection, long input, unsafe control characters, OCR response shape, allowed special keys, background low-level blocking, background stale-server gating, and repeated same-key forwarding for command-capable vision messages. |
| Vision-only tmux command output can be reconstructed from OCR pages in supported local macOS tmux surfaces, currently Terminal.app and Ghostty windows, rather than direct tmux buffer access. | `tests/vision_messages.test.js` | Covers a successful fake-helper `vision-tmux-ocr-run-line` through known-exit done-marker wait, copy-mode pagination, OCR page stitching, nonzero exit propagation, start-marker detection, temporary high tmux history limit reporting, conservative pagination-end detection, OCR page failure reporting, repeated-page lossy/truncation reporting, lossy cleanup/truncation reporting, missing-start truncation, and max-page truncation; also covers bbox-based OCR row reconstruction, OCR-safe tmux status marker matching, unknown marker-status handling, PageDown key validation, cleanup of tmux control/status lines, supported-app filtering, unsupported-app rejection, and command wrapping that preserves marker execution when the command contains `#`; `scripts/run_terminal_vision_self_test.sh` is a manual macOS GUI token-loop check for screenshot/OCR/input. |
| WebSocket text frame parsing handles fragmented, coalesced, extended-length, masked client, and server frames, and rejects announced messages above 2 MiB before buffering their payload; a client refresh/reset while work continues cannot turn a late result write into an uncaught server error. | `tests/server_websocket_frames.test.js` | Covers low-level frame decoder/encoder behavior, the pre-payload transport resource bound, destroyed sockets, and synchronous reset errors during late response delivery. |
| Real-browser extension tests load unpacked builds across Chrome's loader transition: Chrome 116-136 use the legacy startup switches, while Chrome 137+ uses the unsafe-authorized null-delimited CDP pipe and directly calls `Extensions.loadUnpacked` before page automation. | `tests/cdp_pipe_client.test.js`, `tests/chrome_extension_e2e.test.js` | Covers split and coalesced null-delimited frames, unsolicited events, CDP errors, pending-request rejection on Chrome exit, fixed extension-ID assertion, and a complete Chrome 150 browser run through the modern pipe loader. |
| Chrome extension e2e flow works with a real unpacked extension and Chromium-family browser, including compact-panel defaults and expansion, output-idle alerts and controls, default no-target shell helpers, intentional deletion of pending output, composer redraw, refresh recovery, clean unpresented-result replay, local-only delivered duplicates, a long MV3 WebSocket, large shell bodies, file helpers with and without `AI_HELPER_FILE_PATH`, persistent role routing and role-scoped reset, local multi-agent workflows, optional focused Draw.io-only/Skills-only runs, isolated one-case-per-tab Skill dispatch pages, a host-mapped M365 compatibility page with isolated `/chat` to `/chat/<UUID>` route cases, and screenshots. | `tests/chrome_extension_e2e.test.js` | Proves only More is visible at healthy idle startup, every advanced control remains grouped and expandable, Force appears for a runnable rendered helper, Stop replaces it during a real run, then verifies the common Stop is hidden while adjacent `Continue waiting` and `Stop helper` controls are visible, clicks Continue, observes the next idle alert, clicks its contextual Stop, and verifies exit 130/interruption output before restoring the 3-minute default. It also generates deterministic, tightly cropped panel documentation images through a temporary real-DOM screenshot stage, exercises repeated Draw.io render/failure/recovery, canonical Skill code split by host layout, malformed Skill response queuing with the composer removed, stale weak-input displacement after the composer returns, and exact one-message recovery; creates unique independent tabs for a host-mapped M365 flattened prompt/Copilot list/catalog/ACK chain, M365 first-UUID-route Skill load, live new-chat Skill load, route-assigned same-root completion after Stop removal, queued-result route recovery, cold-history redraw/manual recovery (including redraw during unrelated generation), late asynchronous historical hydration after a state-confirmed empty lifecycle, stale old-route Stop rejection, and explicit-user rejection with isolated page identities and pending state; emits detailed Draw.io/Skill delivery state on timeouts; and covers page-session clearing with retained extension role, agent-only reset preserving default panes, pending output deletion, composer redraw, route change, long-helper refresh recovery, service-worker duration, a 9000-character script with bounded `cmdHash`, the default temporary-HOME Downloads destination, a managed-server restart with an override destination and no Downloads copy. |
| A live helper result already written into a chat composer may survive one proven host-independent provisional-to-opaque-id assignment, including ChatGPT `/` to `/c/<id>` or `/uc/<id>` and M365 `/chat` to `/chat/<UUID>`, only through its exact retained helper/turn guard. The old actuator is invalidated, a React-style composer replacement must still contain the complete plugin-owned text, and recovery is send-only: no backend replay and no second composer write. | `tests/content_route_delivery.test.js`, `tests/content_last_shell_call_candidate.test.js`, `tests/chrome_extension_e2e.test.js` | Unit cases cover exact-text continuation, user-draft cancellation, submitted-unconfirmed recovery, queued-result carry, and restored-entry rejection. Dedicated real-Chrome ChatGPT and M365 pages block the first send attempt, replace the composer with exact plugin-owned text, assign the permanent conversation URL, then prove one backend execution, one insertion, one submitted reply, and an empty pending queue; retained in-flight, replaced-transcript, query/permanent-route, and copied-node cases cover adjacent negative boundaries. |
| Release packaging creates matching source and extension archives for the selected manifest version; a caller-supplied version must match that manifest, clean releases package `HEAD`, while `ALLOW_DIRTY=1` local packages include the current worktree plus untracked non-ignored files. The extension archive includes the Draw.io UI, pinned viewer, Apache-2.0 license, and attribution. | `scripts/package_release.sh`, `tests/package_release_version.test.js` | Rejects mislabeled release assets, builds and inspects the extension ZIP for all Draw.io runtime/licensing files, guards against stale source archives during local release validation, and keeps generated zip checksums under `dist/release/v*/SHA256SUMS.txt`. |
| Feature/test coverage documentation stays in sync with all committed automated test files. | `tests/feature_test_matrix.test.js` | Fails if any `tests/*.test.js` file is missing from this matrix or if this matrix is not referenced from project docs. |
| Full automated checks and tests can be run with one command on macOS and Ubuntu. | `scripts/test_all.sh`, `tests/feature_test_matrix.test.js` | The runner performs JavaScript syntax checks, per-script shell syntax checks, `git diff --check`, every Node test, and the Chrome extension e2e test. |
| Current ChatGPT authored-message recognition is host-scoped to exact outermost `li[data-message-role="user|assistant"]` roots. User submission proof reads exactly one directly-owned `[data-user-message-copy]`; assistant helper scanning reads only the directly-owned `[data-assistant-markdown]`. Nested role/legacy-role spoofs, duplicated user-copy bodies, user content, and sponsored/ad siblings fail closed or remain outside helper text. | `tests/content_skill_load_dispatch.test.js`, `tests/content_last_shell_call_candidate.test.js`, `tests/chrome_extension_e2e.test.js` | Focused cases cover exact ChatGPT user/assistant roots, canonical payload projection, submitted-user counting, assistant helper detection, and ad-insensitive Skill ownership, with negatives for nested current/legacy role spoofing, ambiguous user payload bodies, sponsored helper text, later user turns, changed canonical payload, and non-ChatGPT hosts. An independent host-mapped `chatgpt.com` real-Chromium page uses the current exact selectors and real MutationObserver/composer submission path to prove cold-history, user/nested/sponsored negatives and one live shell-helper positive exactly once. |
| ChatGPT's in-place composer actuator morph from exact `Send message` to `Stop generating` is generation evidence even when no control node is added. A first `/` to `/uc/...` or `/c/...` new-chat route may preserve only the exact current final adjacent user/assistant turn. The completed-helper tail is bounded to eight seconds on this strict ChatGPT proof so the scanner's quiet settles do not expire a valid response; expired, historical, nonfinal, rewritten, copied, or second-route helpers remain inert. Skill backend/delivery ownership rebinds across ChatGPT response-root redraw only through unchanged canonical user/assistant IDs, exact user payload, exact semantic envelope, and at most the plugin's own newly submitted reply. | `tests/content_skill_load_dispatch.test.js`, `tests/content_last_shell_call_candidate.test.js`, `tests/chrome_extension_e2e.test.js` | Covers Send→Stop attribute mutation positives plus authored-message spoof negatives, first new-chat route assignment, bounded eight-second final-helper recovery and expiration, exact final adjacency, no historical borrowing, stable-ID Skill root replacement, sponsored-sibling mutation tolerance, exact plugin-reply continuation, and invalidation by a later/different user message, changed ID/payload/helper, second route, or stale generation. |
| A restrictive host `frame-src 'self'` must not strand Draw.io in staging. When the current document exposes a valid `script-src` nonce, the extension builds a fixed sandboxed `srcdoc` that carries only a generated channel plus nonce-authorized packaged CSS/renderer scripts; raw Draw.io XML still travels only over the channel-bound `postMessage` request. Nonce/channel validation and HTML-attribute escaping fail closed, the iframe omits same-origin access and referrer disclosure, and hosts without a usable nonce retain the packaged extension-page fallback. The viewer accepts either the fixed srcdoc metadata channel or the legacy URL-hash channel. | `tests/drawio_preview.test.js`, `tests/chrome_extension_e2e.test.js` | Focused tests accept only a nonce from `script-src`, reject short/wrong-directive/attribute-breaking nonce and channel input, verify all three packaged resources carry the exact nonce, and execute the metadata-channel viewer path. A dedicated one-case page sends `Content-Security-Policy: frame-src 'self'` and proves one ready `nonce-srcdoc` current frame, zero staging/errors/AI replies/shell calls, and an enabled download; the ordinary page separately retains the extension-URL path. |
