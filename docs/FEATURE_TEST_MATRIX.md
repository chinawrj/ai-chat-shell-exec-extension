# Feature Test Matrix

This table is the required map between product features and automated test cases. When a feature or test case is added, removed, or renamed, update this file in the same change.

`./scripts/test_all.sh` is the single command for the full automated suite. It runs static checks, shell syntax checks, whitespace checks, every `tests/*.test.js` file, and the Chrome extension e2e test.

| Feature or invariant | Test cases | Coverage notes |
| --- | --- | --- |
| Root and `extension/` manifests stay behaviorally aligned for either unpacked load path, Chrome 116 is the minimum supported browser, and user-visible content/background/release versions stay unified. | `tests/manifest_consistency.test.js` | Compares manifest version, minimum Chrome version, key, permissions, background worker, popup, content script wiring, content script version, background version message support, shell-health startup checks, `tmux-ensure`/`tmux-reset-forai` wiring, state-health wiring, changelog entry, and release-note file. |
| Default enabled hosts and automatic chain limit stay aligned across background, content, and popup code. | `tests/default_enabled_hosts.test.js` | Enforces `chatgpt.com`, `m365.cloud.microsoft`, and `maxChainCalls = 100` in all duplicated defaults. |
| Background settings migration preserves existing user choices while migrating legacy defaults. | `tests/background_settings_migration.test.js` | Covers legacy enabled-host and chain-limit migration behavior. |
| Background WebSocket forwarding passes file, shell, board, and status messages only after server protocol health succeeds; long shell and board channels use 20-second activity heartbeats and no browser runtime watchdog, while read-only `run-status` recovery never creates a new execution claim. | `tests/background_write_file_message.test.js` | Covers write-file and command payload shapes, success handling, repeated same-key forwarding, non-blocking audit ledger updates, protocol health gating, heartbeat send/cleanup behavior, shell/board watchdog disabling, and status-result audit updates without command replay. |
| Background board-helper message handling forwards board commands over WebSocket only after server protocol health passes, and records ledger state without duplicate blocking. | `tests/background_write_file_message.test.js` | Covers `run-board` payload shape, named board forwarding, success response handling, target recording, repeated same-key forwarding, and protocol mismatch blocking without sending WebSocket payloads. |
| Background agent-hub message handling forwards only allowlisted `agent-*` messages over WebSocket after server protocol health passes. | `tests/background_agent_message.test.js` | Covers `agent-register`, `agent-register-tmux-ai`, `agent-send`, `agent-poll`, `agent-ack`, and `agent-reply` forwarding through the existing health gate, plus rejection of unsupported agent message types before WebSocket forwarding. |
| Local agent hub keeps an in-memory roster and mailbox for multi-tab master/slave experiments. Agent delivery and helper scanning share exact composer ownership without treating pending UI work as command execution. | `tests/agent_hub.test.js`, `tests/content_agent_delivery.test.js`, `tests/content_last_shell_call_candidate.test.js`, `tests/chrome_extension_e2e.test.js` | Covers agent registration, listing, pending counts, send, poll, ack, heartbeat and stale pruning, tab-local activation, content re-registration, pending delivery UI, strict composer/send association, atomic draft preservation, reload-safe sent-but-unacked state, profile-change cancellation, helper deferral, exact-text ownership, and response-loss idempotency: an identical `messageId` plus semantic payload returns the original web/tmux-ai envelope without redelivery, while changed payload conflicts. Also covers invalid IDs/roles/recipients, local bridge flows, panel setup, master delegation, slave composer delivery, and browser proof that quoted reply templates do not auto-send placeholder results. |
| Tmux AI agents can register an explicit tmux pane as a master/slave runtime, receive task prompts, and reply through the CLI instead of tmux output parsing. | `tests/tmux_agent_integration.test.js`, `tests/agent_reply_cli.test.js`, `tests/background_agent_message.test.js`, `tests/content_agent_delivery.test.js`, `tests/chrome_extension_e2e.test.js`, opt-in `tests/real_claude_tmux_slave_e2e.test.js` | Covers `surface: tmux-ai`, `replyMode: cli`, master floating-panel registration of tmux-ai slaves through the real Refresh dropdown path and roster state, self-explanatory registration errors for missing master/slave id/tmux target, agent setup Check diagnostics for missing slaves, browser-only ready states, ready tmux-ai configurations, stale tmux-ai panes, and re-registration hints for stale pane ids, async roster validation that marks missing tmux-ai panes as `stale` and `canReceiveTask=false`, send-time stale marking for unavailable tmux targets, tmux target validation, prompt delivery into a registered pane, per-task short reply script generation, delivery metadata that exposes reply body file, reply script file, short reply command, full CLI command for diagnostics, and next step, reply body-file validation, CLI WebSocket payload forwarding, CLI failure hints and next actions, `agent-reply` insertion into the master mailbox, pending web-page delivery until the recipient page is ready, preservation of unreplied tmux-ai tasks during mailbox pruning for long-running work, duplicate reply rejection, wrong-recipient/task-id reply rejection, browser e2e coverage for web master panel -> tmux-ai slave -> CLI reply -> web master delivery, and opt-in proof with a real Claude Code tmux pane. |
| Project-level Claude Code skill guides tmux-hosted Claude slaves to complete tasks and report back through the short reply script. | `tests/claude_skill.test.js` | Covers `.claude/skills/tmux-ai-slave-reply/SKILL.md` frontmatter, auto-trigger description, reply-file/short-reply-command workflow, preference for the short script over reconstructing long CLI flags, `ok: true` completion gate, and duplicate/error handling guidance. |
| Extension release version, server protocol version, helper protocol version, and visual protocol metadata stay separate; core diagnostics remain concise when vision is unused, while server health still exposes visual metadata for vision-capable flows. | `tests/protocol_metadata.test.js`, `tests/background_write_file_message.test.js`, `tests/background_vision_message.test.js`, `tests/popup_config.test.js`, `tests/manifest_consistency.test.js` | Covers `serverProtocolVersion = 6`, `helperProtocolVersion = 2`, `visualProtocolVersion = 1`, release metadata in `/health`, background rejection of old, missing-helper, or helper-mismatched servers before shell/file/board/vision WebSocket forwarding, concise popup health text when vision is unavailable, and source guards for floating-panel protocol status text. |
| Plain text shell helper parsing accepts `ai-helper-shell-start`, command body, and `ai-helper-shell-end`; every line between start and end is command text, so heredocs and `cat <<EOF` file creation are preserved. AI-facing helper examples use four-backtick fences while rendered marker-only blocks remain supported, and closed four-backtick fenced shell helpers can recover from a missing `ai-helper-shell-end`. | `tests/content_plain_text_shell_call.test.js` | Covers parsing, extracted helper blocks inside surrounding text and four-backtick markdown examples, no-target shell helpers, legacy target-looking lines treated as command text, heredoc command preservation, validation, command body preservation, fenced missing-end recovery, matching payload hashes with explicit end markers, and non-recovery when either fence boundary is missing. |
| Plain text file helper parsing accepts `ai-helper-file-start`, filename, exact content, and `ai-helper-file-end`; closed four-backtick fenced file helpers can recover from a missing `ai-helper-file-end`. | `tests/content_plain_text_shell_call.test.js` | Covers JSON-like file content as plain text, trailing blank lines, file validation, file helper result formatting, and fenced missing-end recovery with trailing blank content preservation. |
| Plain text board helper parsing accepts `ai-helper-board-start`, exactly one board command line, and `ai-helper-board-end`; named markers such as `ai-helper-board-R1-start` route to `board-R1`; closed four-backtick fenced board helpers can recover from a missing end marker. | `tests/content_plain_text_shell_call.test.js` | Covers default and named board parsing, identity suffixes, semantic key differences, named end-marker matching, fenced missing-end recovery, empty command rejection, multiline rejection, and copied-helper-marker rejection. |
| Plain text agent-message helper parsing accepts `ai-helper-agent-message-start`, `to` and optional `task-id` headers, a blank-line-separated body, and `ai-helper-agent-message-end`; closed four-backtick fenced agent-message helpers can recover from a missing end marker. | `tests/content_plain_text_shell_call.test.js` | Covers agent-message parsing, identity suffixes, semantic key differences, safe recipient validation, task-id validation, empty body rejection, fenced missing-end recovery, helper detection hints, slave reply prompt formatting with non-executable quoted helper markers, default master agent ID suggestions, and roster summary formatting. |
| AI master pages can autonomously discover available slave agents and inspect delegated task state through read-only `ai-helper-agent-roster-*` and `ai-helper-agent-task-status-*` helpers. | `tests/content_plain_text_shell_call.test.js`, `tests/content_agent_delivery.test.js`, `tests/content_last_shell_call_candidate.test.js`, `tests/agent_hub.test.js`, `tests/background_agent_message.test.js`, `tests/chrome_extension_e2e.test.js` | Covers roster/status helper parsing, filters, validation, candidate detection hints, rejection of malformed query helpers, repeatable query behavior, suppression of query helpers embedded inside `shell-output`, background forwarding of `agent-list` and `agent-task-status`, server `agent-list` capability metadata including `surface`, `replyMode`, `pendingCount`, `canReceiveTask`, `capabilities`, `lastSeenAgeMs`, `stale`, and `staleReason`, content dispatch from helper to hub query, formatted `shell-output` for roster and task status without executable follow-up helper blocks, roster next actions that only recommend slaves with `canReceiveTask=true`, browser e2e proof that roster/status helpers work from a registered master tab, and AI-oriented recovery hints for missing recipients or stale tmux-ai panes. |
| JSON helper requests and old `ai-helper-start-shell` aliases remain unsupported. | `tests/content_plain_text_shell_call.test.js` | Covers language-hint rejection and shell-call validation rejection for old markers. |
| Optional helper identity suffixes distinguish request payload identities for parsing and diagnostics, but do not control server execution dedup. | `tests/content_plain_text_shell_call.test.js`, `tests/chrome_extension_e2e.test.js` | Covers suffixed shell and file parsing, semantic key differences, malformed suffix rejection, unsuffixed payload hashes, removal of frontend shell-output command dedup, and a real suffixed shell e2e request. |
| The content script scan-debounces the exact rendered helper request and never treats command text, a prior semantic key, cancellation/failure, or historical `shell-output` as execution proof. Once a shell/board run has been attempted, an ambiguous runtime/status-recovery failure remains local-only and cannot reopen that same rendered request; once a backend response exists, a bounded per-tab pending-result queue retries only strict composer delivery and never resends that rendered command. Recycled/new render roots remain eligible for backend adjudication. Page-lifecycle tokens isolate SPA transitions, and composer auto-send may act only while it owns exact text in the current composer. | `tests/content_plain_text_shell_call.test.js`, `tests/content_last_shell_call_candidate.test.js`, `tests/content_force_run.test.js`, `tests/chrome_extension_e2e.test.js` | Covers historical output followed by a new helper, virtualized replacement and same-Element recycling, identical helpers in a shared container, SPA identity, hidden stale controls, lifecycle invalidation, execution-lock release, shell and board response-loss recovery exhaustion without composer/model feedback or same-render command replay, persistent local delivery after composer/send failure and reload, no same-render backend resend, strict CRLF/NBSP/blank-paragraph-only ownership equivalence, nonempty line/internal-space distinction, saved send-button current-composer validation, bounded send attempts, Force run, and a real browser proving a newly rendered identical helper still reaches the server. |
| Shell-output formatting is compact while preserving command/execution identity, interruption, clean recovery, retryability, and per-pane queue metadata. Authoritative duplicate-control responses format to an empty string as a defense in depth and never enter model-facing output. | `tests/content_shell_output_format.test.js`, `tests/content_last_shell_call_candidate.test.js` | Covers short, long, and multiline commands, `cmdHash`, `executionId`, timeout/process state, Ctrl+C, queue metadata, clean `recovered: true` output, local-only already-presented duplicates, and removal of `duplicate`, `skipped`, replay, reason, and previous-call diagnostics from an unpresented stored-result recovery. |
| Browser/background ledgers are non-blocking audit records, while the shell server is the sole duplicate authority after resolving the actual tmux pane. Only a prior server-proved execution with the same tmux server/pane + current pane-shell PID + command + actual-cwd fingerprint can be skipped; Ctrl+C after execution starts is history, while pre-execution interruption, running, failed, timed-out/unconfirmed, unavailable-target, generic-board, different/recreated/respawned-pane-shell, incomplete-identity, and unscoped audit calls never become authority. A canonical execution identity survives duplicate chains; per-tab/page presentation receipts remain durably pending until acknowledged, canonical pending deliveries coalesce by `executionId`, and a bounded local presentation tombstone suppresses stale unpresented replay responses after local delivery. Already-presented duplicates stay local-only, while a genuinely unpresented bounded result is recovered cleanly without duplicate metadata. | `tests/background_write_file_message.test.js`, `tests/background_vision_message.test.js`, `tests/server_dedup_force.test.js`, `tests/server_result_recovery.test.js`, `tests/content_last_shell_call_candidate.test.js`, `tests/tmux_helpers.test.js`, `tests/tmux_forai_setup_integration.test.js`, `tests/tmux_shell_lifecycle_integration.test.js`, `tests/tmux_board_integration.test.js`, `tests/tmux_visual_run_integration.test.js`, `tests/chrome_extension_e2e.test.js` | Covers immutable attempt and canonical execution identities, terminal monotonicity, interrupted completion, same-pane duplicates, presentation receipts created before and after persisted duplicate attempts, failed receipt persistence/retry without composer reinsertion, same-`executionId` local pending coalescing, reload-persistent local presentation suppression, local-only delivered duplicates, clean unpresented replay, replay expiry, nonterminal retention, kind-scoped read-only status across fresh server load, different cwd/pane/recreated pane, real `respawn-pane -k` shell replacement with a stable pane id, missing pane-shell metadata, board fail-open, Force bypass, and browser-to-server final adjudication without internal duplicate feedback to the model. |
| Force call (`callMeta.force=true` or top-level `force=true`) records `forced: true` and explicitly bypasses server-confirmed completed execution dedup. | `tests/background_write_file_message.test.js`, `tests/server_dedup_force.test.js`, `tests/tmux_visual_run_integration.test.js` | Covers force metadata recording, payload forwarding, top-level force consistency, ledger `forced` fields, and actual rerun after a completed same-pane execution. |
| Manual `Force run` execution preserves pending forced retry behavior, user-facing force button metadata, and selects the latest assistant helper block even when that message quotes prior `shell-output`. | `tests/content_force_run.test.js`, `tests/content_last_shell_call_candidate.test.js` | Source-level guard for force-run function names plus runtime candidate-selection regression coverage for mixed quote+helper messages, pure quoted output, active-call pending force retention, and force scan dispatching the newest helper cmd. |
| Floating panel "detected helper" debug view always reflects the most recent helper block, including while the chat thread is still streaming or before the quiet-period gate fires. | `tests/content_last_shell_call_candidate.test.js` | Covers the post-scan debug update plus a streaming-phase regression that asserts the panel shows the newest helper cmd and not an earlier one when scan early-returns due to thread text changes. |
| Popup settings UI can load, save, normalize enabled sites, list tmux targets, display default `ForAI` host/board/cwd state, and export/import portable config without ledgers. | `tests/popup_config.test.js` | Covers tmux target display, default workspace summary, site normalization, config export/import, clamping, and exclusion of command ledgers. |
| Tmux target parsing and resolution accept pane id, `session:window.pane`, and unique window names while rejecting ambiguous names. | `tests/tmux_helpers.test.js` | Covers pane parsing, target resolution, tmux socket argument building, and tmux environment socket extraction. |
| Board helper target selection, prompt extraction, normalized pipe-log capture, and examples stay stable. Delivery uses the immutable-pane queue and persistent lease, reads a stable prompt without injecting a probe Enter, submits literal text plus Enter atomically, and retains ownership after crossing the response timeout. Runtime loss recovers through kind-scoped read-only status and never resends the same rendered board helper. Shell-backed panes add process-group proof; generic TUI prompt evidence stays non-authoritative for dedup. | `tests/tmux_helpers.test.js`, `tests/tmux_board_integration.test.js`, `tests/server_result_recovery.test.js`, `tests/background_write_file_message.test.js`, `tests/content_last_shell_call_candidate.test.js` | Covers default/named/configured targets, errors, examples, validation, prompt capture, truncation, busy rejection, long follower serialization, prompt spoof protection, generic-TUI limitations, server-restart persistent-owner result reconstruction, completed bounded board status recovery with empty execution key, channel-loss polling without another `run-board`, and explicit repeated board execution. |
| Board helper tmux integration probes the prompt before sending commands and refuses promptless panes. Shell-backed boards combine prompt and process-group evidence; a generic non-shell TUI has no authoritative completion primitive, so prompt text is neither a strong serialization guarantee nor duplicate proof, and board execution dedup always fails open. | `tests/tmux_board_integration.test.js` | Uses a private tmux socket with fake `board` and `board-R1` panes to cover prompt-based capture, repeated same command execution, cwd changes, timed-out retry, shell-backed prompt-spoof output before actual completion, the documented generic-TUI limitation, unavailable targets, and probe failure without sending the command. |
| Page startup and tmux target listing ensure a default `ForAI` tmux session with `host` and `board` windows; new default windows use the configured cwd; reset recreates only the default `ForAI` session; shell helpers always run in `ForAI:host`, and board helpers default to `ForAI:board` unless a named board marker selects `ForAI:board-<suffix>`. | `tests/tmux_forai_setup_integration.test.js`, `tests/tmux_helpers.test.js`, `tests/content_plain_text_shell_call.test.js`, `tests/popup_config.test.js`, `tests/manifest_consistency.test.js`, `tests/feature_test_matrix.test.js` | Uses a private tmux socket to cover automatic session/window creation, realpath cwd policy, idempotent setup, reset with and without an existing session, default shell execution, heredoc file creation followed by later commands in one run, ignoring legacy shell target fields, default and named board target resolution, parser support for targetless shell helpers, popup default summary, content/background reset wiring, exact tmux session targeting so `ForAI` does not match `ForAI-*`, and docs/agent-guide protection against reintroducing the obsolete shell target line. |
| Agent pages can route shell helpers to an isolated per-agent tmux workspace while non-agent pages continue to use `ForAI:host`; execution dedup is scoped to the resolved pane instance. | `tests/tmux_forai_setup_integration.test.js`, `tests/chrome_extension_e2e.test.js` | Covers `agentId: slave-a` mapping to `ForAI-slave-a:host`, exact tmux target handling with both default and per-agent sessions present, the same command executing once in each distinct pane, a reset/recreated default pane executing it again, and local test page e2e proof that an agent-profiled tab runs shell helpers in the per-agent tmux session before unregistering and returning to the default non-agent path. |
| Runtime state directory health is required before shell server startup and command execution; project `.state` is the default runtime state directory, safe conflicts are auto-repaired with `.broken-*` backups, `AI_CHAT_SHELL_STATE_DIR` remains an explicit override, and macOS LaunchAgent auto-start stays removed. | `tests/state_dir.test.js`, `tests/manifest_consistency.test.js`, `scripts/doctor.sh`, `scripts/test_all.sh` | Covers default state path selection, missing state auto-creation, state path as a file auto-repair, required subpath conflict auto-repair, corrupted ledger backup and recovery, state-log path repair, installer compatibility cleanup plus foreground launch, unwritable state where supported, health fields, content/background health wiring, and shell syntax checks for startup/cleanup scripts and state-log handling. |
| Server command claims are not left stuck as `running` when post-claim execution throws. A request waiting for a pane first receives a persistent, non-authoritative queued reservation; the initial persistent pane-owner claim is bound to that immutable attempt before authoritative queue-head adjudication, nonterminal attempts survive ledger count pruning, and late cleanup cannot downgrade terminal completion. | `tests/server_dedup_force.test.js`, `tests/server_result_recovery.test.js`, `tests/tmux_shell_lifecycle_integration.test.js` | Covers post-claim failure cleanup, initial owner binding to the reservation, same-process fresh-module restart recovery of the persistent lease while the original handler continues, release-CAS failure preservation and later recovery of completion proof, queued `run-status` visibility with empty pre-adjudication cwd/execution key, same-attempt queue-head adjudication, retention through terminal-entry churn, completed-state monotonicity, duplicate replay completion of a reservation, leader/follower execution, and an identical failed retry returning `run`. |
| Doctor diagnostics check Node.js, tmux, default `ForAI` host/board visibility, manifest ID, server origin policy, server protocol version, helper protocol version, state directory health, and default cwd validity without mutating tmux state. | `tests/protocol_metadata.test.js`, `scripts/doctor.sh`, `scripts/test_all.sh` | Automated coverage keeps doctor protocol expectations aligned with server/background metadata; shell syntax checks cover the script, while manual doctor runs exercise live tmux/server state and environment overrides. |
| Internal direct tmux visual-run test mode shares the persistent per-pane shell queue without sending a preemptive Ctrl+C, clears pane history only after acquiring ownership, executes one command through `/bin/sh -c`, marks completion via tmux window name, and dumps the tmux buffer only when `AI_CHAT_SHELL_ENABLE_DIRECT_VISUAL_TMUX=1` is set. | `tests/tmux_visual_run_integration.test.js` | Uses a private tmux socket to cover default production rejection, explicit test-mode enablement, shell-helper serialization/non-preemption, atomic literal+Enter, recovery of stale pre-dispatch ownership without executing buffered text, failed-ledger cleanup when execution/completion proof is missing, `&&`/`||` compatibility, nonzero exit propagation, stale history removal, done-window marker detection, capture-pane output, completed same-pane duplicate adjudication, timed-out execution retry, Force run bypass, and single-line validation. |
| Default tmux target failures produce a helper-readable failure without requiring AI helper blocks to name a target. | `tests/tmux_helpers.test.js` | Covers default target failure response shape and example helper block. |
| File helpers write only a single file name under the Downloads directory. | `tests/tmux_helpers.test.js` | Covers safe path resolution, traversal rejection, byte counts, and file content writes. |
| Tmux shell execution uses pid/status-aware foreground waiting plus a tokenized ownership lease persisted on each pane: shell state timeout is not a runtime limit, long-running commands continue while their runner process is alive, refreshed-page and restarted-server helpers wait without being typed into an observed busy pane, cwd and duplicate claims are captured only at the revalidated queue head, direct Terminal vision runs share ownership without preemption, different panes remain concurrent, and Ctrl+C before or after the executed marker returns promptly with the correct retry/completion verdict. Readiness requires tmux PID/TTY plus foreground-process-group proof and fails closed/retryable when metadata is unavailable. Queue identity remains stable socket + tmux-server PID + pane id, while duplicate identity additionally includes the current pane shell PID so respawned shells start with fresh execution history. Root-interactive-shell blocking builtins remain an explicitly documented limitation without shell-hook integration. Literal text plus Enter is one tmux client transaction. | `tests/tmux_helpers.test.js`, `tests/tmux_forai_setup_integration.test.js`, `tests/tmux_shell_lifecycle_integration.test.js`, `tests/tmux_visual_run_integration.test.js`, `tests/content_shell_output_format.test.js`, `tests/background_write_file_message.test.js`, `tests/server_result_recovery.test.js`, `tests/chrome_extension_e2e.test.js` | Covers stable socket + server-pid + pane-id queue keys, address changes, server-restart owner adoption, real `respawn-pane -k` shell replacement without duplicate-history inheritance, real nested child jobs through exit/Ctrl+C, missing process metadata, root-shell builtin boundary, atomic literal+Enter, queue-head cwd, runner state files and signal traps, pre-marker retry, bounded output extraction, long-command follower queues, concurrent panes, tmux replacement safety, status recovery without resending, refresh-time queued work plus clean unpresented-result recovery, canonical receipt-aware duplicate adjudication, real bounded-latency `C-c`, and timeout/process/interruption/queue/recovery metadata. |
| macOS UI-window vision control exposes structured health, local Terminal/Ghostty visual tmux candidate discovery, and high-level visual run requests by default; generic window listing/capture/type/key, message-level OCR, and Terminal self-test primitives require `AI_CHAT_SHELL_ENABLE_LOW_LEVEL_VISION=1` for local manual tests. | `tests/vision_messages.test.js`, `tests/background_vision_message.test.js` | Uses a fake macOS vision helper to cover low-level default rejection, low-level explicit test-mode validation, helper missing, non-macOS, permission denied, invalid window, local visual candidate filtering for Terminal.app/Ghostty, unsupported app rejection, app mismatch rejection, long input, unsafe control characters, OCR response shape, allowed special keys, background low-level blocking, background stale-server gating, and repeated same-key forwarding for command-capable vision messages. |
| Vision-only tmux command output can be reconstructed from OCR pages in supported local macOS tmux surfaces, currently Terminal.app and Ghostty windows, rather than direct tmux buffer access. | `tests/vision_messages.test.js` | Covers a successful fake-helper `vision-tmux-ocr-run-line` through known-exit done-marker wait, copy-mode pagination, OCR page stitching, nonzero exit propagation, start-marker detection, temporary high tmux history limit reporting, conservative pagination-end detection, OCR page failure reporting, repeated-page lossy/truncation reporting, lossy cleanup/truncation reporting, missing-start truncation, and max-page truncation; also covers bbox-based OCR row reconstruction, OCR-safe tmux status marker matching, unknown marker-status handling, PageDown key validation, cleanup of tmux control/status lines, supported-app filtering, unsupported-app rejection, and command wrapping that preserves marker execution when the command contains `#`; `scripts/run_terminal_vision_self_test.sh` is a manual macOS GUI token-loop check for screenshot/OCR/input. |
| WebSocket text frame parsing handles fragmented, coalesced, extended-length, masked client, and server frames; a client refresh/reset while work continues cannot turn a late result write into an uncaught server error. | `tests/server_websocket_frames.test.js` | Covers low-level frame decoder/encoder behavior plus destroyed sockets and synchronous reset errors during late response delivery. |
| Chrome extension e2e flow works with a real unpacked extension, Chromium-family browser, default no-target shell helper, page refresh during a long helper, clean unpresented-result recovery, local-only delivered duplicates, a command longer than the MV3 30-second idle threshold, file helper, local multi-agent workflows, and optional screenshots. | `tests/chrome_extension_e2e.test.js` | Launches or reuses the server/test page, exercises browser and tmux-ai agent paths, starts a long helper and refreshes before a queued successor, verifies the old never-presented stdout is recovered exactly once without re-execution or duplicate metadata, verifies a later already-presented identical command changes neither user-message count nor composer and is explained only in the panel, crosses the service-worker idle threshold, checks default target output and file helper behavior, and writes optional screenshots. |
| Release packaging creates matching source and extension archives for the selected manifest version; a caller-supplied version must match that manifest, clean releases package `HEAD`, while `ALLOW_DIRTY=1` local packages include the current worktree plus untracked non-ignored files. | `scripts/package_release.sh`, `tests/package_release_version.test.js` | Rejects mislabeled release assets, guards against stale source archives during local release validation, and keeps generated zip checksums under `dist/release/v*/SHA256SUMS.txt`. |
| Feature/test coverage documentation stays in sync with all committed automated test files. | `tests/feature_test_matrix.test.js` | Fails if any `tests/*.test.js` file is missing from this matrix or if this matrix is not referenced from project docs. |
| Full automated checks and tests can be run with one command on macOS and Ubuntu. | `scripts/test_all.sh`, `tests/feature_test_matrix.test.js` | The runner performs JavaScript syntax checks, per-script shell syntax checks, `git diff --check`, every Node test, and the Chrome extension e2e test. |
