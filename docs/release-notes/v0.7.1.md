# AI Chat Shell Exec v0.7.1

This patch release focuses on making the multi-agent and tmux-ai workflows easier to follow from the README.

## Changes

- Adds copyable browser master/slave prompts directly in the README.
- Adds browser-tab and web master + tmux Claude quick starts with smoke tests.
- Clarifies that `claude.ai` must be manually enabled before use.
- Documents the Claude Code project skill fallback and the short per-task reply script workflow.
- Adds multi-agent troubleshooting guidance for missing slaves, pending delivery, terminal-only tmux replies, and stale `reply-to` failures.

## Verification

- `./scripts/test_all.sh`
