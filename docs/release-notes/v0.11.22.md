# AI Chat Shell Exec v0.11.22

Fixes Master pages repeatedly receiving “still waiting” chat messages before a Slave has replied.

- `waiting-for-recipient-poll`, `delivered-waiting-for-reply`, and `waiting-for-tmux-ai-reply` now update only the extension panel, including repeated status queries and Force run.
- Actual Slave replies, query failures, and states after a reply still reach the chat. Master instructions and task receipts explain that replies arrive automatically without repeated status queries.
- Refresh discards unwritten queued waiting-status messages saved by earlier versions. Already-inserted text retains its existing delivery ownership.
- Positive and negative regression cases cover waiting, completion, failures, Force, auto-send, legacy snapshots, and real reply delivery; real Chrome checks repeated queries cause zero composer mutations or chat submissions.

Update the unpacked extension and refresh affected chat tabs. No server protocol or permission changes are required.
