# AI Chat Shell Exec v0.3.1

README helper examples patch.

## Highlights

- Clarifies the README introduction so the first usage section lists all supported helper types.
- Adds top-level examples for shell, board, and file helpers.
- Keeps AI-facing wording explicit: one fenced `text` code block, no prose.

## Install Or Upgrade

1. Download and unzip `ai-chat-shell-exec-extension-v0.3.1-source.zip`.
2. Open `chrome://extensions`, enable Developer mode, and load the extracted project root or `extension/`.
3. Restart the local shell server from the extracted project root.

   On macOS:

   ```sh
   ./scripts/install_shell_server_agent.sh
   ```

   On Ubuntu, or for a temporary foreground server:

   ```sh
   ./scripts/start_shell_server.sh
   ```

4. Reload enabled chat tabs.
5. Copy the updated AI instructions from the README or `docs/AI_INSTRUCTIONS.md`.
