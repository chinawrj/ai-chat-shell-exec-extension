# AI Chat Shell Exec v0.8.2

This patch release removes backend duplicate-call blocking for command execution.

## Changes

- Removes duplicate/running/completed execution blocking from the extension background worker.
- Removes duplicate/running/completed execution blocking from the local shell server.
- Keeps command ledgers as audit records only; repeated same-key requests still run.
- Stops treating backend `duplicate/skipped` responses as a silent no-output success path in the content script.
- Preserves only the minimal frontend same-page scan loop guard, while `Force run` remains available for manual reruns.

## Verification

- `./scripts/test_all.sh`
